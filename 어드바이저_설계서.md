# Arcana Codex 어드바이저 — 빌드 설계서 (Claude Code용)

토대: 오픈소스 `https://github.com/yard9/STS2-Advisor-Y` (MIT 라이선스).
이 repo의 `db.js`(카드 데이터)와 `logic.js`(추천 로직)를 토대로, 아래 화면과 기능으로 다시 구성한다.
LICENSE 파일과 원작자(Shawnrai) 표기는 유지한다.

---

## 1. 화면 구조 (3패널 + 상단 탭) — 참고 UI 형식 반영

> 참고 이미지의 레이아웃/스타일을 따른다 (다크 테마, 카드 그림 중심). 단, 참고 UI엔 유물이 없으므로 오른쪽에 **유물 칸을 새로 추가**한다.

- **상단:** 캐릭터 탭 — 아이언클래드 · 사일런트 · 디펙트 · 네크로바인더 · 리젠트. 각 탭에 아이콘 + 색상 구분. 누르면 해당 캐릭터 기준으로 전환.

- **왼쪽 패널 "카드 선택":**
  - 상단에 **아키타입 필터 칩**: 취약 덱 · 타격 덱 · 소멸 덱 · 자해 덱 · 방밀 덱 · 무한 압축 덱 · 무한 덱 · **현재 덱 기준**(기본 선택). 칩에 현재 덱과의 매칭 %를 함께 표시.
  - **보상 카드 / 상점 구매** 두 하위 탭. **탭에 따라 카드 슬롯 개수가 다름:**
    - **보상 카드 = 3칸** (카드 1·2·3)
    - **상점 구매 = 7칸 고정** (카드 1~7, **무색 카드 포함**)
  - **각 슬롯("카드 N")을 누르면 → 카드 검색 모달(새 창)이 뜬다.** (아래 참고 스크린샷 형식)
    - 모달 상단: 이름/초성 검색창(예: "폼멜타격 → ㅍㅁㅌㄱ") + 필터 탭(전체 / 현재 캐릭터 / 중립 / 다른 직업).
    - 카드 그리드(그림 + 이름 + 타입 + 등급 배지)에서 검색·선택.
    - **카드를 고르면 그 슬롯이 채워지고, 자동으로 다음 슬롯(카드 N+1) 모달로 넘어간다.** 마지막 슬롯이면 모달 닫힘.
  - 채워진 슬롯의 카드는 그림 + 이름 + 등급으로 표시, × 로 제거 가능.

- **가운데 패널 "추천 결과":**
  - 추천 카드를 **순위대로** 나열. 각 항목: 카드 그림, 한글 이름, 배지(빌드 후보 / 초반 안정성 / 중복 등), 등급 배지, **한글 이유 문장**, 그리고 "① 이 카드 뽑기" 버튼. **1순위는 초록색으로 강조.**
  - **뽑기 동작:** "이 카드 뽑기"를 누르면 → 그 카드가 **오른쪽 현재 덱에 추가** → **왼쪽 입력 카드 + 가운데 추천 결과를 비운다(초기화)** → 다음 보상 입력 대기 상태.
  - 맨 아래 **스킵** 영역: 스킵이 나은지 판단 문구 + "스킵하기" 버튼. **스킵 시엔 덱에 추가하지 않고 입력/추천만 초기화.**

- **입력·검색 (공통):** 카드/유물 이름 입력칸은 모두 **한글 초성 검색** 지원. (예: "ㅍㅅ" → "포식", "ㄱㅅ" → "거상") 입력한 초성이 이름 각 글자의 초성과 일치하면 자동완성 후보로 노출.

- **오른쪽 패널 "현재 유물 + 현재 덱":**
  - **현재 유물 (NEW):** 보유 유물을 아이콘으로 나열 + "＋추가" 칸(이름 입력 → 자동완성 → 추가). 참고 UI에 없던 부분으로 새로 넣는다.
  - **현재 덱 N장** + 초기화 버튼. 카드 그림 그리드(강화 레벨 배지 표시). 그리드 끝에 "＋추가" 칸으로 카드 계속 추가.
  - 카드에 **강화 우선** / **제거 추천** 배지 표시.
  - 유물·카드는 런 도중 계속 추가/삭제 가능하고, 변경 시 추천이 즉시 갱신된다.
  - 유물은 추천에 큰 영향을 주므로 추천 점수에 반영하고 **가운데 이유 문장에서 언급**한다. (예: "악마의 형상 보유 → 근력 카드 가치 ↑")

## 2. 데이터 — 게임에서 직접 추출 + 자동업데이트 〔현재 위상: 백업(2차 소스)〕

> **⚠️ 2026-07-05 갱신:** 1차 데이터 소스는 **spire-codex API**로 확정됐다(→ §「데이터 층 아키텍처」). 이 절이 설명하는 GDRE/ILSpy 직접 추출은 **백업(2차 소스)으로 강등**된 상태이며, spire-codex가 중단될 때 자체 가동하기 위한 안전망 문서다. 아래의 "확정"·"지시" 표현은 당시 기준이고 현재 주력 경로가 아니다. 특히 **§2.3의 ◆ 문제는 API가 수치 완성본 설명을 제공하면서 원천 소멸**했다.

모든 카드·유물·고대의 존재(Ancient)는 **그림 + 한글 이름 + 한글 효과 설명**으로 표시.
데이터는 내 게임 설치 폴더에서 직접 추출하고, **게임이 패치되면 자동으로 감지해 다시 추출**한다. (STS2는 Godot 4, 데이터는 `.pck` + `sts2.dll`에 내장)

**2.1 초기 추출 (1회 셋업)**
- GDRE Tools "Recover Project"로 STS2 실행파일 옆 `.pck`를 풀어 이미지 + 로컬라이제이션(한글 포함) 확보.
- 필요 시 ILSpy로 `sts2.dll` 디컴파일해 수치 데이터 확보.
- 파싱해서 `data/cards.json`, `data/relics.json`, `data/ancients.json`(한글명·설명·이미지경로·비용·타입·희귀도 등) 생성, 이미지는 `assets/`로 복사.

**2.2 자동업데이트 감시**
- 앱 실행 시(또는 주기적으로) STS2 버전을 확인 — 설치 폴더의 `release_info.json` 또는 `.pck` 수정시각 사용.
- 저장해둔 버전과 다르면 → **자동으로 재추출 → JSON/이미지 갱신 → 앱이 다음 로드부터 최신 데이터 사용.**
- 추출은 시간이 걸리니 **백그라운드로**(UI 안 멈춤), 실패 시 **직전 성공 데이터 유지**(폴백).

**2.3 카드 설명 숫자 바인딩 (◆ 문제 해결 — 중요)**
- STS2 설명은 SmartFormat 템플릿이라 `{0}` 같은 **자리표시자**로 되어 있다. (예: "피해를 {0} 줍니다")
- `.pck`의 설명 템플릿(한글)과 `sts2.dll`의 **카드 수치(데미지·방어·코스트·강화값)**를 반드시 **합쳐서** 렌더링해야 한다. 안 하면 숫자 자리가 ◆ 등 빈 기호로 뜬다.
- **지시:** `sts2.dll`에서 수치를 추출했는지 확인하고, 설명 템플릿의 자리표시자에 실제 숫자를 바인딩해 "피해를 8 줍니다"처럼 렌더링. **강화 카드는 강화된 수치**로 채운다. (코스트·데미지·방어 전부 동일 처리)

**2.4 주의**
- 게임 폴더 접근 권한 필요(File System Access API / Tauri) — 한 번 허용.
- 완전 자동 백그라운드 감시는 **Tauri(.exe)** 단계에서 가장 안정적. 웹앱 단계에선 "앱 열 때 버전 확인 → 필요 시 재추출"로 시작.
- STS2 얼리액세스 → 파일 구조가 바뀔 수 있으니 오류 처리를 튼튼하게.
- 이미지·게임 텍스트는 Mega Crit 저작물 → 저장소에 복사본을 두지 않고 **spire-codex CDN 핫링킹으로만 표시**(재배포 아님).
- **2026-07-20 Mega Crit 허가 확인**(NOTICE 참조): 2026-07-18 문의(devs@megacrit.com)에 대한 회신으로, 게임을 수정하지 않는 외부 컴패니언 툴로서 카드 이름·설명·이미지 표시 허용 + 기부 기반 선호 확인 → 위 방식의 미확정 사항 해소.

## 3. 추천 로직 (두뇌) — 데이터 기반으로 전면 재설계

> **방향 전환(2026-07-04):** 기존 "규칙 기반 점수"는 고수·커뮤니티 티어와 어긋나서 폐기. 대신 **외부 고수/커뮤니티 데이터를 기본 점수로 삼고, 내 상황에 맞게 보정**하는 구조로 간다. 목표 우선순위: **① 판단 대행(정확히 찍어줌) → ② 학습 → ③ 지식 이전.**

### 3.1 구조 — "기본 점수판 → 맞춤 보정"

```
[B. 승률 DELTA·커뮤니티 점수] ─┐
                              ├→ 카드별 "기본 점수/티어" ─→ [C. 내 덱·유물·맥락 보정] ─→ 최종 추천 + 한글 이유
[A. 전문가 티어·해설]         ─┘   (토대)                      (가중치 최대)
```

- **재료 = A + B (기본 점수판).** 보정하려면 먼저 각 카드의 기본 평가가 있어야 함 → 이게 바닥.
- **요리법 = C (맞춤 보정).** 중요도 최상위지만, 혼자 못 서고 바닥(A·B) 위에서만 작동.
- **중요도 순위:** C > B > A. **만드는 순서는 반대:** 먼저 A·B로 바닥, 그 위에 C.

### 3.2 세 데이터 소스

**A. 전문가 티어·해설 (기본 점수 + 이유 텍스트)**
- 사람이 판단한 S~D 등급 + "왜 이 티어인지" 해설.
- 후보 출처(조사됨): Mobalytics, slaythetierlist.com(390장 전 카드, 패치별 갱신), nat1gaming(카드별 상세 해설, 최신), slaythespire2.space, frvr 등.
- 용도: 기본 점수의 한 축 + **최종 추천의 "한글 이유 첨언"** 재료(→ 3순위 지식 이전 실현).

**B. 실제 런 성과 통계 (결과 데이터 = 기본 점수의 단단한 바닥)**
- 카드를 넣은 런과 안 넣은 런의 **승률 차이(DELTA)**, 그리고 소표본을 Bayesian 보정한 커뮤니티 점수(0~100).
- **pick rate 단독 사용 금지** — 안전한 카드로 쏠리는 선택 편향이라 실제 카드 가치와 어긋난다. 참고 지표로만 쓴다.
- 출처(채택됨): spire-codex API — `/api/runs/stats`(DELTA), `/api/runs/scores`(Bayesian 점수).
- 용도: A의 주관을 보완하는 객관 지표. B를 더 신뢰(우선순위 B > A).

**C. 내 덱·유물·맥락 보정 (가중치 최대)**
- 현재 덱 아키타입, 보유 유물, (가능하면)막·층·체력을 반영해 기본 점수를 조정.
- 예: 기본 B티어라도 내 아키타입에 완벽히 맞으면 ↑, 기본 S티어라도 내 덱과 안 맞으면 ↓.
- 유물 연동: "악마의 형상 보유 → 근력 카드 가치 ↑" 식으로 점수·이유에 반영.

### 3.3 출력

- 추천 카드 옆에 **점수 숫자가 아니라 사람이 읽는 한글 이유** + 근거 배지(예: "고수 S티어", "pick rate 상위", "내 아키타입 적합").
- 강화 우선순위·제거 추천도 같은 점수판에서 산출.

### 3.4 데이터 수집·갱신 (솔직한 주의)

- 스크랩은 **각 사이트 약관·robots·공개 범위 안에서만.** 과도한 수집·재배포 금지. 개인용 우선.
- STS2 얼리액세스 → 티어·pick rate가 패치마다 바뀜 → **주기적 재수집** 필요(§2 자동업데이트와 별개의 "메타 데이터" 갱신).
- 표본 편향 주의(고수·특정 커뮤니티 쏠림). 상관 ≠ 인과 — 근거로 제시하되 단정 금지.
- 출처가 서로 다를 수 있음 → 최소 A 1곳 + B 1곳으로 시작, 충돌 시 B(행동) 우선 + A(해설)로 설명.

### 3.5 단계

- **1단계:** A 1곳 + B 1곳을 골라 카드별 기본 점수판 구축(규칙 기반 두뇌 교체).
- **2단계:** 그 위에 C(내 덱 맞춤 보정) 로직 + 한글 이유 생성.
- **3단계(향후):** `docs/기능_패턴학습.md`의 성공 런 데이터로 C를 고도화(맥락 정밀화).

## 4. 자동화 — 단계별 (중요)

**Phase 1 (먼저):** 위 화면을 완성. 보상 카드는 **빠른 수동 입력**(카드 그리드에서 클릭 / 이름 타이핑 자동완성). 현재 덱도 우선 수동 입력 가능.

**Phase 2:** **현재 덱 자동 입력.**
- STS2 진행 중 세이브 위치(예시): `C:\Users\(사용자명)\AppData\Roaming\SlayTheSpire2\steam\(스팀ID)\profile1\saves\`
- 먼저 **세이브 파일 하나를 열어 현재 덱이 어디에 어떤 형식으로 들어있는지 확인**하고, 그걸 읽어 오른쪽 "현재 덱" 패널을 자동으로 채운다.
- 웹앱이 디스크를 읽으려면 폴더 접근 권한이 필요(File System Access) → 한 번 허용 후 앱 열 때마다 갱신. 진짜 백그라운드 자동 감시는 Tauri(.exe) 단계에서.

**Phase 3 (스트레치, 선택):** 보상 카드까지 자동 = 게임 화면 인식(이미지 인식) 필요. 고급·불안정. 당장은 안 함.

## 5. 패키징

- Phase 1~2를 웹앱으로 완성 후 → **PWA**(시작메뉴 설치 + 오프라인).
- 백그라운드 자동 감시가 필요해지면 → **Tauri로 .exe** 전환.

## 6. 두 번째 메뉴 — "런 복기 / 실패 분석"

어드바이저 화면과 별도로 **상단에서 전환하는 두 번째 메뉴**. 지난 런을 불러와 "왜 졌는지"를 한글로 피드백한다.

**흐름:** 게임 런 히스토리(`...\saves\history`)에서 지난 런들 불러오기 → 자동 분석 → 한글 피드백 카드.

**분석 4가지 (모두 포함):**
1. **덱 구성 문제** — 화력/방어/저주·저해 밸런스, 덱 크기, 아키타입 완성도. (예: "공격 카드 3장뿐 → 화력 부족")
2. **사망 지점 분석** — 어느 막·적·엘리트·보스에서 졌는지, 반복되는 사망 패턴. (예: "3막 보스에서 반복 사망")
3. **카드 선택 복기** — 런 기록의 카드 선택(픽/스킵) 로그를 훑어 아쉬운 선택 지적. (게임 기록에 픽/스킵이 남아 있으면 활용)
4. **유물 활용도** — 보유 유물 중 아키타입과 안 맞거나 못 살린 유물 지적.

**단계적 구현:**
- **지금(규칙 기반):** 런 기록에 든 정보만으로 위 4가지를 판단. 정확한 인과는 아니지만 유용한 패턴 지적 가능. 런 기록만 있으면 바로 구현.
- **나중(성공 사례 대조):** 커뮤니티 성공 런 데이터가 쌓이면 "비슷한 덱으로 이긴 사람은 이렇게 했다" 식 고도화. (§7 데이터 수집 이후)

**주의:** 규칙 기반 피드백은 "보조 조언"이다. 인과 단정 금지, 근거와 함께 제시.

## 7. (향후) 커뮤니티 성공 패턴 학습 — 별도 Phase

- 유튜브 설명/댓글의 덱 코드, Reddit/커뮤니티 성공 런 게시물 등에서 데이터 수집.
- "덱+유물 조합 → 승률/도달층" 태깅 → 현재 덱과 패턴 매칭해 근거 기반 추천/피드백.
- 크롤링·정규화·로직 재설계가 필요한 큰 작업 → **Phase 1 완성 후 별도 설계.**

---

## Claude Code 작업 순서 제안

1. repo를 받아 `index.html`을 띄워 현재 동작 확인.
2. 화면을 스크린샷 형식대로 재배치: 상단 캐릭터 탭 + 왼쪽 아키타입 필터/카드 선택 + 가운데 순위 추천 + 오른쪽 **현재 유물 줄(NEW) + 카드 그리드**.
3. **데이터 추출 파이프라인 (길 B):** GDRE Tools로 `.pck` 추출 → (필요 시 ILSpy로 `sts2.dll`) → `data/*.json` + `assets/` 이미지 생성. Claude Code가 설치·실행을 자동화하고 비개발자에겐 클릭 단위로 안내.
4. **자동업데이트 감시:** 앱 로드 시 STS2 버전 확인 → 변경 시 백그라운드 재추출(논블로킹, 실패 시 폴백) → 최신 데이터 반영.
5. 카드·유물·고대의 존재에 추출된 그림 + 한글 이름/설명 입히기. **설명의 SmartFormat 자리표시자에 `sts2.dll` 수치를 바인딩해 실제 숫자로 렌더링(◆ 방지), 강화 카드는 강화 수치로.**
6. 추천 카드에 "한글 이유" 표시. 오른쪽 패널에 강화/제거 배지. 유물을 추천 점수·이유에 반영.
7. 보상 카드 빠른 입력(그리드 클릭/자동완성) 구현. (Phase 1 완료 지점)
8. 세이브 파일에서 현재 덱 자동 읽기 (Phase 2).
9. PWA → 필요 시 Tauri(.exe)로 전환하며 백그라운드 자동감시 강화.

---

## 방향성 확정 (2026-07-05)

### 제품 방향
- **1차 핵심 역할:** 실시간 코칭 — 런 도중 카드 픽 판단.
- **입력 방식:** 수동 우선 → (UI 안정화 후) 완전 자동. 단계적 전환.
- **배포·수익 모델:** 무료 공개 + **Ko-fi 기부 전용, 광고 없음 — Mega Crit 공식 확인 완료 (2026-07-20)**. 확인 기록은 NOTICE.
  - 외부 컴패니언 툴 허용·기부(donation) 기반 선호를 Mega Crit로부터 이메일로 확인받음. 유료화·광고는 하지 않음.

### 추천 엔진 — 기본점수
- **기본점수 = 커뮤니티 통계의 승률 DELTA**(넣었을 때 승률 − 안 넣었을 때 승률) 중심.
  - 픽률 단독 금지(안전 카드 편향). DELTA가 실제 카드 가치에 가까움.
- 전문가 티어표는 **보정·소표본 대비용**으로 상위에 얹음(출처 표기).
- 그 위에 **맥락 보정**(현재 덱 아키타입·유물·상황) — **최상위 가중치**.

### 데이터 층 아키텍처 (신규 확정)
- **정적 카드 데이터 + 통계 = spire-codex(ptrlrd/spire-codex)를 1차 소스로 채택.**
  - 근거: STS2 디컴파일→API 오픈소스. 나와 동일한 GDRE+ILSpy 파이프라인. 한국어 포함 14개 언어 API(`/api/cards?lang=kor`). 통계 엔드포인트(`/runs`, `/meta`). 매일 유지보수(별 228, 최신 v1.1.3). 셀프호스팅 가능.
- 내 GDRE/ILSpy 직접 추출 = **백업으로 강등.** spire-codex 중단 시 자체 가동 가능(안전망).
- 통계 소스 최종 선택은 검증 후: spire-codex `/meta` vs ststracker.app 중 품질 확인 후 택1.
- **원칙:** 데이터 층은 '유지되는 외부 소스 = 완료'로 취급, 에너지는 어드바이저(점수 로직·시너지 감지·UX)에 집중.

#### API 검증 결과 (2026-07-05) — spire-codex 단독 채택 확정
- **Base URL:** `https://spire-codex.com/api` · 무인증 · rate limit 60req/분 · **CORS `*`**(정적 웹앱 브라우저 직접 호출 가능, 프록시 불필요) · `Cache-Control max-age=300`.
- **카드 데이터:** `GET /api/cards?lang=kor` — 577장 전부 한글(이름·설명), 설명은 **수치 완성본**(◆ 문제 원천 소멸) + `description_raw` 원본 병행. `powers_applied`·`is_x_cost`/`star_cost`·`upgrade`/`upgrade_description`·`image_url` 등 구조화 필드 풍부. → 자체 `db.js`/`card_stats`/`loc`/`card_art` 대체.
- **기본점수(DELTA):** `GET /api/runs/stats?character=…&ascension=…` → `top_cards[]`의 `win_runs`/`total_runs_with`로 **승률차 DELTA 계산**. `pick_rates[]`는 별도(단독 사용 금지). 캐릭터·승천·모드 필터 지원.
- **미리 계산된 티어:** `GET /api/runs/scores/{cards|relics|potions}` → Bayesian 보정 `score`(0–100)·`elo`·`win_rate`. 소표본 대비 안전판으로 병용.
- **최신성:** 실시간 런 수집(초 단위), 최신 `build_id v0.108.0`(내 게임과 동일). 베타 빌드는 `beta.spire-codex.com` 별도.
- **주의:** DELTA 선택편향(고수가 늦게 얻는 강카드 과대) → 전문가 티어표 상위 보정 + 표본수 하한($n \ge$ 임계) 적용. `runs/scores`의 Bayesian score가 소표본 완충 역할.
- **자체 GDRE/ILSpy 추출:** 백업으로 강등(스냅샷 유지). spire-codex 중단 시 셀프호스팅(오픈소스)으로 승격.

#### 구현 결과 (2026-07-05) — 두뇌·등급 확정
- **스냅샷 빌드:** `tools/build_from_api.js` → `data/api_data.js`(카드 577·유물 296·Codex 티어·캐릭터별 승률통계 전체+최고승천 A10). `file://` 더블클릭 유지 위해 스냅샷 방식(갱신=재실행). CORS 열려 있어 정적 호스팅 시 직접 fetch도 가능.
- **기본점수(티어 등급):** = **Codex 커뮤니티 점수(0~100→/20) × 0.7 + 전문가 티어(db.js) × 0.3.** DELTA 단독은 +편향(중앙값 Δ+15%p)이라 A 쏠림 → Codex(Bayesian, 17~100 분산)로 교체하니 **배지 S~D 고르게 분산**(실측 S13/A29/B26/C14/D18%). Codex 미커버(~6%)는 DELTA→전문가 티어로 폴백.
- **맥락 보정:** 위 기본점수에 아키타입·유물·역할·층·인카운터·승천 보정(기존 `logic.js` scoreCard). **DELTA(승률차)**는 추천 근거로 표시("커뮤니티 승률 +X%p · 넣은 런 N판").
- **승천 밴드:** 상단 '승천' 선택기(전체 / 최고 A10). A10 표본<200 카드는 전체로 폴백.
- **카드 사실 override:** `koName`/설명/이미지가 API 우선(카드+유물). 검색(i18n)·저장키는 유지.

#### 구현 결과 (2026-07-06) — 투명성·폴백·정리
- **등급 출처 표기 UI:** 추천 카드마다 근거 하단에 'ⓘ 등급 산정: …' 한 줄(`gradeSourceNote`, `.reason-src`). 산정 경로를 그대로 노출 — Codex 티어 있으면 '커뮤니티 통계 spire-codex 70% + 전문가 티어 Shawnrai 30%', DELTA 보정이면 '커뮤니티 승률 spire-codex + 전문가 티어 Shawnrai 보정'. (§3.2 '출처 표기' 방침 이행)
- **무데이터 폴백 메시지:** 커뮤니티 통계(Codex·DELTA)가 전혀 없는 카드는 '아직 커뮤니티 통계가 없는 카드예요 — 전문가 티어 Shawnrai 평가만 반영했습니다'로 표시(`isExpertOnly`, `.reason-src.nostat`). 빈칸 오해 방지. 실측: 카드풀 581장 중 무데이터 42장(유령17+상태저주15+토큰10), 보상으로 뽑히는 일반 카드는 사실상 전부 커버.
- **백업 파이프라인 정리:** 게임 직접추출(GDRE/ILSpy) 스크립트 8개를 `[역할: 백업(2차 소스)]`로 표기 + `tools/README.md` 지도. 주력은 `build_from_api.js`(API).
- **유령 카드 확인:** STS2 미존재 STS1 잔재 카드는 `data/extra_cards.js`의 `STALE` 목록이 로드 시 이미 제거 중. 전수검사로 검색 유령 0장 확인(추가 조치 불요).
