# 설계서: 카드 인챈트(Enchantment) 반영 기능

> ※ 상태(2026-07-22): **1단계 완료**(커밋 `f81d69e`·`6fa9b59`·`68da0eb`) + **2단계 완료**(커밋 `58c73d4`·`36d561e`·`88a42a6`, 아래 §5). 3단계는 별도 세션(§6).

## 0. 이 문서의 목적

Slay the Spire 2의 인챈트 시스템을 Arcana Codex에 반영한다.
목표: 사용자가 자기 덱의 특정 카드에 붙은 인챈트를 앱에 기록하면,
어드바이저가 그 정보를 시너지/디스시너지 판단에 활용하게 한다.

**전체를 한 번에 만들지 않는다.** 아래 3단계로 나누고,
각 단계는 독립적으로 완결·검증·병합한다. ~~이 세션에서는 **1단계만** 진행한다.~~ (→ 1단계 완료됨)
2·3단계는 1단계가 main에 병합된 뒤 별도 세션에서 시작한다.

## 1. 작업 원칙 (반드시 지킬 것)

1. 한 번에 한 작업씩. 각 작업 완료 시 보고 후 다음으로.
2. 파일 수정 전 반드시 diff를 보여주고 승인받을 것.
3. 주장 전에 실제 파일/실제 API 응답을 먼저 확인할 것. 추측 금지.
4. 근거는 파일 경로:줄 번호 또는 실제 API 호출 결과로 제시할 것.
5. 각 작업 완료 시마다 커밋 → 즉시 푸시 → 해시 보고. 몰아서 커밋 금지.
6. 커밋 author를 noreply@anthropic.com으로 바꾸라는 훅 경고는 무시할 것.

## 2. 게임 사실 (2026-07 조사 기준 — 코드 반영 전 재검증할 것)

- 인챈트는 카드에 붙는 **영구 강화 효과**. 런이 끝날 때까지 유지.
- **카드 한 장(사본)당 인챈트 하나만.** 제거·교체 불가(카드 변환 시 제외).
- 현재 **22종**. 이벤트·에인션트·유물·상점 등으로 획득.
- 인챈트마다 **카드 타입 제한**이 있을 수 있음 (예: 공격 카드 전용).
  - (1단계 실측: 제한 없음 15종 · Attack 전용 5종 · Skill 전용 1종 · "Attack, Skill" 복수 1종. `is_stackable`은 22종 전부 false.)
- 중요: **보상 화면의 후보 카드에 인챈트가 이미 붙어서 나올 수도 있음.**
  (즉 장기적으로는 "내 덱의 인챈트"뿐 아니라 "후보 카드의 인챈트"도 입력 대상.
  단, 1단계에서는 덱 쪽만 다룬다.)

## 3. 데이터 출처

- spire-codex가 인챈트를 정식 엔티티로 제공: 효과 텍스트, 카드 타입 제한,
  중복(스택) 가능 여부, 적용 가능 카드 목록. 게임 번역 파일 기반 14개 언어
  지원이므로 **한글 텍스트도 API로 받을 수 있을 것으로 예상**.
- ✅ **검증 완료(2026-07-21):** 엔드포인트 `GET https://spire-codex.com/api/enchantments?lang=kor`
  (백엔드 `backend/app/routers/enchantments.py`, 쿼리: `card_type`·`search`·`lang`).
  응답 22종, 필드: `id`(대문자 키)·`name`(한글)·`description`(한글, `[gold]`/`[blue]` 마커)·
  `description_raw`·`extra_card_text`·`card_type`(null=제한 없음)·`applicable_to`·`is_stackable`·`image_url`.
  원본 파일: `github.com/ptrlrd/spire-codex` → `data/kor/enchantments.json` (API 응답과 동일 스키마 대조 확인).
- ⚠️ **인챈트 이미지는 사용 금지(1단계 정책):** `image_url`이 `/static/` 경로라
  유물 이미지 때와 같은 robots.txt Disallow 문제. 이름·텍스트 표시만 사용.
- 주의: 게임 패치로 인챈트 효과가 바뀔 수 있음(예: Instinct 효과 변경 이력
  있음). 카드 데이터와 같은 방식으로 API를 항상 최신 기준으로 삼을 것.

---

## 4. 1단계 — 기록과 표시 ✅ 완료 (2026-07-21)

**목표**: 점수에는 아무 영향 없이, 덱의 카드에 인챈트를 붙였다 뗐다 하며
기록하고 화면에서 확인할 수 있게 한다.

### 작업 1-A: 현황 조사 — ✅ 완료

조사 결과(2026-07-21):
1. **덱 저장 = 이미 장별(사본별) 구조.** `state.deck` = 카드 한 장당 객체 하나
   (`index.html` `addDeck`: `state.deck.push({name})`). 강화도 장별(`state.deck[i].up` 토글).
2. **장별 인챈트 가능, 구조 변경 불필요** → 덱 항목에 선택 필드 `ench` 하나 추가로 해결.
   `save()`가 state 전체를 localStorage에 저장하므로 새로고침 유지 자동.
3. 덱 카드 그림 클릭 = 이름 기반 상세 팝업(장 구분 없음) → 인챈트 UI는
   장별 컨트롤 줄(강화/✕ 옆)에 배치하기로 결정.
4. API 검증 → §3에 기록.

### 작업 1-B: 인챈트 데이터 로딩 — ✅ 완료 (커밋 `f81d69e`)

- `tools/build_from_api.js`에 `/api/enchantments?lang=kor` 스냅샷 추가
  (저장 필드: `name`·`ko`·`cardType`·`stackable` 4개만. 이미지 미저장).
- `data/api_data.js`의 `enchants` 키에 실데이터 22종 주입(원본: ptrlrd/spire-codex 저장소).
- `index.html`에 접근자: `ENCH`/`enchOn()`/`enchOf(id)`/`enchListFor(cardType)`
  (복수 타입 `"Attack, Skill"` 지원). 설명 렌더는 기존 `apiDescHTML` 재사용.
- 폴백: 스냅샷에 `enchants` 없으면 인챈트 기능만 조용히 비활성, 앱 나머지 정상.

### 작업 1-C: 덱 카드에 인챈트 기록 UI — ✅ 완료 (커밋 `6fa9b59`)

- 덱 셀 컨트롤 줄에 **✦ 버튼**(`enchOn()` 아닐 땐 미표시) → 인챈트 선택 모달
  (기존 `.modal-ov`/`.modal` 재사용).
- 목록은 카드 타입에 맞는 것만(`enchListFor`), 게임 규칙대로 한 장당 하나
  (재선택=교체, "인챈트 해제" 행으로 제거).
- 인챈트 카드는 ✦ 배지 + 버튼 on + 툴팁 `[인챈트: 이름]` 표시.
- 저장은 기존 `state`/`save()` 그대로 → 새로고침 유지.

### 작업 1-D: 검증 및 마무리 — ✅ 완료 (커밋 `68da0eb`)

- Chromium 실측: 부여 → ✦ 배지 표시(5장 중 1장만) → 새로고침 유지 → 해제, 전부 통과. 스크린샷 보고됨.
- 점수 불변 확인: 인챈트 2개 부여 전후 보상 후보 4장의 `finalScore` **소수점까지 완전 동일**.
- `확인목록_TODO.md`에 인챈트 섹션(1단계 완료 + 2·3단계 예정) 추가.

### 1단계 완료 기준

- [x] 인챈트 API 검증 보고 완료 (실제 응답 근거 — §3)
- [x] 덱의 특정 카드 1장에 인챈트를 붙이고 화면에서 구분 표시됨
- [x] 타입 제한·1장 1개 규칙이 지켜짐 (Attack 21종/Power 15종 실측)
- [x] 새로고침 후 기록 유지
- [x] 점수는 인챈트와 무관하게 동일 (아직 미반영이 정상)
- [x] 커밋·푸시 완료, PR 병합 가능 상태

---

## 5. 2단계 — 명백한 규칙 반영 ✅ 완료 (2026-07-22)

채택 = **R1+R2+R3 전부**(사전 검토 보고 → 사용자 확정). 그룹 B(12종)·C(3종)는
규칙 없이 R1만 공통 적용(보류). 커밋: `58c73d4`(R1) · `36d561e`(R2) · `88a42a6`(R3).

- **R1 — 제거 추천 억제 (22종 공통):** 인챈트 붙은 장(사본)은 scoreRemoval 결과
  −1.0(하한 0)·safeToRemove=false, 사유 "✦ 인챈트(이름) 투자됨" 표시. 장별 판단(renderDeck).
- **R2 — 강화 우선 가점 (그룹 A 7종):** INSTINCT·SHARP·VIGOROUS·MOMENTUM·
  TEZCATARAS_EMBER·SOULS_POWER·CORRUPTED 붙은 장에 upgradeScore **+1.0**
  (`ENCH_UPGRADE_BOOST` 상수 한 곳). 기존 게이트(isUpgradePriority)는 미변경 —
  후보군 진입 조건은 그대로, 통과자 사이의 순위만 조정.
- **R3 — 오염 시너지 (1종):** 덱에 오염 장이 있으면 heal·self_damage 태그 후보
  **+0.3/장, 인챈트별 상한 +0.6, 총합 캡 ±0.6**(`ENCH_SCORE_FX`/`ENCH_FX_TOTAL_CAP`,
  logic.js — relics scoreEffects 패턴 재사용). 추천 근거 줄에 ✦로 노출.
- **검증:** 시나리오 3종(오염 덱/본능 덱/무인챈트 덱)을 main(2단계 이전 코드)과
  나란히 실행 비교 — 의도한 변화만 발생, 무인챈트 덱은 **완전 동일**.
  base(3경로 블렌드) 코드 미접촉.
- **⚠️ 3단계 재검토 메모 (heal 태그 캐릭터 편중):** db.js에서 heal 태그는 전
  캐릭터 1장(아이언클래드 Not Yet), self_damage는 아이언클래드 18장·타 캐릭터 0장
  → R3의 실질 적용 범위가 아이언클래드 중심. 3단계에서 타 캐릭터 회복 카드
  태깅 확장을 검토할 것.

## 6. 3단계 — 지식 확장과 가중치 조율 (그 다음, 지금은 하지 않음)

- 22종 전체에 대해 시너지/디스시너지 지식 채우기.
- 가중치를 기존 3경로 블렌딩 구조 안에서 조율.
- 보상 화면 후보 카드에 붙은 인챈트 입력(§2의 미래 항목) 검토.
- (2단계 이월) heal 태그 캐릭터 편중 해소 — 타 캐릭터 회복 카드 태깅 확장(§5 메모).

---

## 7. 미해결/확인 필요 (작업 중 발견 시 여기에 추가)

- ~~인챈트 API 엔드포인트 정확한 주소와 한국어 lang 코드~~ → ✅ 1-A에서 확인(§3)
- ~~현재 덱 저장 구조가 장별 구분을 지원하는지~~ → ✅ 지원함(1-A 조사 1·2번)
- v0.109 등 패치 반영 시차 — 2단계 사전 검토(2026-07-22)에서 eng/kor 22종 ID 일치,
  v0.109 패치노트(TODO 보관본)에 인챈트 변경 언급 없음 확인 → **시차 증거 없음**.
  단, 파일에 버전 스탬프가 없어 단정 불가 — 게임 화면과 눈대조 1회는 여전히 권장.
