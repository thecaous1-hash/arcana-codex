# Spire Codex — 확인 & 수정 목록 (작업 메모)

Claude Code가 바쁠 때 여기 쌓아두고, 여유 생기면 하나씩 물어보고 ☐→☑ 체크.

---

## 🔧 지금 해결해야 할 것

### 1. 카드 설명 숫자가 ◆로 비어서 나옴
- **원인:** STS2 설명은 SmartFormat 템플릿(`{0}` 자리표시자). `.pck`의 한글 템플릿 + `sts2.dll`의 수치를 합쳐야 하는데 안 됨.
- **Claude Code에 물어볼 것:**
  > "카드 설명의 숫자가 ◆로 비어. `sts2.dll`에서 수치(데미지·방어·코스트·강화값) 추출했어? 설명 템플릿의 자리표시자에 실제 숫자를 바인딩해서 렌더링하도록 고쳐줘. 강화 카드는 강화된 수치로."
- ☐ 미해결

### 2. 일부 카드가 옛날 이미지
- **먼저 원인 갈라야 함 (A/B):**
  - A) 게임에서 추출은 했는데 **버전이 옛날** → 재추출하면 해결
  - B) 이미지 출처가 **게임이 아니라 다른 데이터셋/웹** → 출처를 게임 `.pck`로 교체 필요
- **Claude Code에 물어볼 것:**
  > "카드 이미지를 어디서 가져와? 내 게임 `.pck`에서 추출한 거야, 다른 데이터셋이야? 마지막 추출한 STS2 버전이 지금 설치 버전이랑 같아?"
- **근본 해결:** 자동업데이트 감시(§2.2)가 작동하면 패치 때마다 최신 이미지로 자동 갱신 → 재발 방지.
- ☐ 미해결

### 3. 네크로바인더 시작 카드가 이상함
- **원인 후보:** 시작 덱 구성이 잘못 파싱됨(잘못된 카드 포함), 또는 네크로바인더 데이터가 옛날 버전.
- **Claude Code에 물어볼 것:**
  > "네크로바인더 시작 덱이 이상해. 시작 덱 데이터를 `sts2.dll`의 캐릭터 `StartingDeck`에서 정확히 가져왔어? 지금 설치된 게임 버전 기준으로 네크로바인더 시작 덱이 맞는지 확인하고 고쳐줘."
- ☑ **완료(2026-07-04)** — 게임 C# `Necrobinder.cs`의 `StartingDeck` 원문으로 확정. 네크로바인더: `Strike×4·Defend×4·Bodyguard(호위)·Unleash(풀어놓기)` (기존 `Soul Spark`는 존재하지 않는 유령카드 → db.js 이름목록에서도 제거). **보너스:** 전 캐릭터 대조 중 **리젠트도 오류** 발견 → `Strike×4·Defend×4·Falling Star(별똥별)·Venerate(추앙)`로 수정(기존엔 별똥별 누락+타격 1장 초과). 나머지 3캐릭터는 정확했음. (logic.js STARTERS)

### 4. 캐릭터별 기본 카드(타격·수비) 이미지가 안 맞음
- **원인:** STS는 기본 카드(타격·수비)도 **캐릭터별로 아트가 다름.** 지금 모든 캐릭터에 같은 타격/수비 이미지를 쓰고 있을 가능성.
- **Claude Code에 물어볼 것:**
  > "타격·수비 같은 기본 카드는 캐릭터마다 이미지가 달라. `.pck`에 캐릭터별 기본 카드 이미지가 따로 있는데, 지금 캐릭터별로 맞는 이미지를 연결하고 있어? 캐릭터 탭에 맞춰 올바른 기본 카드 이미지가 나오도록 고쳐줘."
- ☐ 미해결

---

## ✅ 이미 설계서에 반영된 개선 (구현 확인 대상)

- ☐ 카드 뽑기 → 오른쪽 덱 추가 + 왼쪽 입력·가운데 추천 초기화 (스킵은 초기화만) — 버튼 존재, 초기화 동작 실행확인 필요
- ☑ **한글 초성 검색 (ㅍㅅ→포식)** — 완료(2026-07-04). i18n.js `nameMatches`에 초성 매칭 추가(공식명+추정명 둘 다 대조). 카드·유물·그리드 검색 전부 적용.
- ☑ **오른쪽 패널 현재 유물 줄 + 추가 칸** — 완료(구현됨)
- ☐ 자동업데이트 감시 (게임 패치 감지 → 자동 재추출) — 도구(check_update.js)만 있고 앱에 미연결

---

## 💡 나중에 추가로 생각해볼 것

- (여기에 떠오르는 아이디어 계속 추가)

---

## 🎨 UI 개선 요청 (설계 반영 대상)

### 5. 유물도 그리드에서 설명 표시
- 카드처럼 **유물 그리드에서 효과 설명**이 보이게. (호버 또는 항목 내 표시)
- **Claude Code에:** "유물도 카드처럼 그리드 영역에서 한글 효과 설명이 보이게 해줘."
- ☑ **완료(2026-07-04)** — 유물을 타일로 바꿔 아이콘+이름+등급+효과설명 인라인 표시. 시작유물 🔒고정, 나머지 ✕제거. ⚠️ 유물 수치는 아직 ◆(유물 stats 미추출 → #1 재추출 때 함께 채우기).

### 6. 보상 카드 입력 영역에 전체 카드 목록이 펼쳐지지 않게
- 지금 "지금 뜬 보상 카드를 위에서 입력하거나 그리드에서 골라넣으세요" 부분에 카드들이 다 열림 → **불필요.**
- 선택한 카드는 **추천 결과 영역에만** 표시. 입력 영역은 검색/입력만.
- **Claude Code에:** "보상 카드 입력 영역에 전체 카드 그리드가 펼쳐지지 않게 해줘. 입력/검색만 두고, 선택한 카드는 가운데 추천 결과에만 띄워줘."
- ☑ **이미 그렇게 동작**(2026-07-04 확인) — 그리드는 "카드 그리드에서 고르기" 버튼으로만 열림(기본 닫힘).

### 7. 카드 타입 표시 (공격/스킬/파워)
- 카드 설명에 **공격 카드 / 스킬 카드 / 파워 카드** 구분 표기.
- 데이터는 `sts2.dll`의 `CardType`에서 나옴.
- **Claude Code에:** "카드마다 공격/스킬/파워 타입을 설명에 표시해줘. (CardType 활용)"
- ☑ **완료**(구현됨) — `typeBadge` 공격/스킬/파워/상태이상/저주 색상 배지.

### 8. 카드 강화 표시
- 카드를 **강화하면 어떻게 바뀌는지** 표시. (강화 전/후 수치 비교 또는 강화 시 변화 표기)
- §2.3 숫자 바인딩과 연결 — 강화 수치도 함께 처리.
- **Claude Code에:** "카드를 강화하면 수치가 어떻게 바뀌는지 강화 표시를 넣어줘. (강화 전/후 비교)"
- ☑ **완료**(구현됨) — `descHTML`이 강화 전→후 수치를 화살표로 표시. (단 ◆ 남는 카드는 #1/#10 참고)

### 9. 현재 덱 카드에 강화 버튼 추가
- 현재 덱의 **각 카드에 강화 버튼(＋)**을 달아, 런 중 강화한 카드를 눌러 반영. 강화하면 그 카드의 수치·표시가 강화본으로 바뀌고, 추천 계산에도 반영.
- 8번(강화 표시)과 연결 — 강화 상태 토글이 곧 강화본 수치 적용.
- **Claude Code에:** "현재 덱의 각 카드에 강화 버튼을 넣어줘. 누르면 그 카드가 강화 상태가 되고, 수치·표시가 강화본으로 바뀌고 추천에도 반영되게."
- ☑ **완료**(구현됨) — 덱 카드마다 `강화` 토글 버튼(state.deck[i].up, 이름에 +).

### 10. 일부 수치가 여전히 ◆로 남음 (1번 후속)
- 1번 바인딩이 **부분적으로만** 적용됨. 특정 카드의 특수 값(두 번째 자리표시자 `{1}`, 조건부/연동 수치 등)이 아직 안 채워짐.
- **Claude Code에:** "아직도 일부 카드 설명에 ◆가 남아 있어. 모든 자리표시자(`{0}`, `{1}`, 조건부·특수 수치 포함)를 빠짐없이 바인딩했는지 점검하고, ◆가 남는 카드들을 찾아서 전부 실제 숫자로 채워줘."
- ☐ 미해결

### 11. 게임 업데이트 후에도 옛날 이미지 (2번 재발 → 자동업데이트 검증)
- 오늘 게임 패치했는데 이미지가 옛날 그대로 = **자동업데이트 감시(§2.2)가 작동 안 함**을 시사.
- **Claude Code에:** "오늘 STS2를 업데이트했는데 카드 이미지가 옛날 거야. 지금 게임 버전으로 데이터·이미지를 다시 추출해서 최신 일러스트로 교체해줘. 그리고 앞으로 게임이 패치되면 자동으로 재추출·갱신되는 자동업데이트 감시가 실제로 작동하는지 확인하고, 안 되면 작동하게 고쳐줘."
- ☐ 미해결

### 12. 카드 클릭 시 상세 팝업
- 카드 그림을 클릭하면 **팝업(모달)**으로: 카드 큰 모습 + 한글 설명 + **강화 시 수치**를 함께 표시.
- 8번(강화 표시)과 연결 — 팝업 안에서 강화 전/후를 보여주면 좋음.
- **Claude Code에:** "카드 그림을 클릭하면 팝업으로 카드 큰 이미지, 한글 설명, 강화하면 바뀌는 수치를 보여줘."
- ☑ **완료(2026-07-04)** — 보상타일/추천카드/덱카드 그림 클릭 시 모달 팝업(큰 이미지 210px + 이름 + 비용/등급/타입 배지 + 한글설명). [기본|강화 후] 토글로 수치가 실제로 바뀜(modalDescHTML=단일값 렌더). ✕/오버레이/ESC로 닫기. ⚠️그리드 카드는 클릭=추가 유지(팝업 안 붙임, 빠른 입력 위해).

### 13. git-credential-manager 오류 (깃허브 업로드 문제)
- `git-credential-manager.exe` → "문자열 바인딩이 틀립니다" 시스템 오류. 깃허브 인증 도구가 손상/오설치된 상태로 보임 → **깃허브 푸시가 막힐 수 있음.** (앱 기능 자체엔 영향 없음)
- **Claude Code에:** "깃허브에 올리려고 하면 `git-credential-manager.exe`에서 '문자열 바인딩이 틀립니다' 오류가 떠. 원인 확인하고 고쳐줘. 클릭 단위로 안내해줘."
- ☑ **완료(2026-07-04)** — 원인: 윈도우 "자격 증명 관리자(VaultSvc)" 서비스가 수동 → 간헐적 RPC 오류. `Set-Service VaultSvc -StartupType Automatic`로 자동 시작 전환(관리자 권한). 명령줄 GCM은 원래 정상이었음.

---

## 🎨 디자인 통일 (별도 작업)

### 14. 하이브리드 디자인 토큰으로 전체 통일
- **컨셉:** 뼈대는 미니멀 테크(거의 새까만 배경·얇은 여백·무채색), **포인트만 게임 무드**(앰버 불씨색). 앰버는 "여기가 중요하다"는 신호로만.
- **Claude Code에:** "아래 CSS 변수(디자인 토큰)로 앱 전체 색·여백·폰트를 통일해줘. 하드코딩된 색은 이 토큰으로 교체."

```css
:root {
  /* 배경 (거의 새까맣게, 살짝만 층 구분) */
  --bg-page:      #0a0a0b;
  --bg-card:      #101012;
  --bg-hover:     #17171a;
  /* 테두리 (얇은 흰색 반투명) */
  --border:       rgba(255,255,255,0.09);
  --border-strong:rgba(255,255,255,0.16);
  /* 글자 (3단계) */
  --text-1:       #f0ede8;   /* 제목·핵심 숫자 */
  --text-2:       #8a8a8f;   /* 보조 설명 */
  --text-3:       #5f5f66;   /* 라벨·날짜·힌트 */
  /* 포인트색 (게임 무드) */
  --accent:       #f0a83c;   /* 앰버 = 긍정·강조 */
  --accent-soft:  rgba(240,168,60,0.12);
  --danger:       #c0392b;   /* 자주 = 패배·경고 (드물게만) */
  --danger-soft:  rgba(192,57,43,0.14);
  /* 형태 */
  --radius-card:  12px;
  --radius-ctrl:  8px;
  --pad-card:     20px;
}
```

- **폰트:** `font-family: 'Pretendard', -apple-system, 'Inter', sans-serif;`
  CDN: `https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/static/pretendard.css` (무료·상업이용 가능, 한글 다크UI에 적합)
- **촌스러움 방지 3규칙:**
  1. **앰버는 화면당 2~3곳만** (핵심 숫자 / 상태 배지 / 주요 버튼). 다 강조 = 강조 없음.
  2. **자주(--danger)는 진짜 부정적일 때만** (패배·삭제·경고). 장식용 금지.
  3. **여백을 아끼지 말 것.** 카드 안쪽 `20px`, 구분선은 있는 듯 없는 듯(`rgba(255,255,255,0.07)`).
- 화면별(런 목록·덱 분석 등)로 안 맞는 부분은 캡처/코드 들고 개별 조정.
- ☐ 미해결
