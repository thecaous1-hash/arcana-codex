# Spire Codex — 확인 & 수정 목록

> 2026-07-05 갱신. 항목 #1~#27을 실제 코드/앱과 대조해 정리했습니다.
> 거의 다 완료(☑). 미완은 **#11 자동업데이트**(웹앱 한계, Tauri 단계 필요) 하나뿐.
> 그동안 **카드 입력 = 슬롯형(#23)**, **런 복기 메뉴(#26)** 로 앱이 크게 발전했습니다.

---

## ✅ 완료

### 데이터 정확도
- ☑ **#1·#10 카드/유물 설명 숫자(◆) 바인딩** — ◆가 카드 269→**14개**(95%↓), 유물 58→**10개**로 감소.
  - 게임 C# 소스에서 수치 재파싱(`parse_card_stats.js` 버그 수정 + `parse_relic_stats.js` 신규).
  - 설명 렌더에 SmartFormat 해석기 도입(조건부 `{IfUpgraded}`/`{InCombat}`, 아이콘 `⚡`/`★`, `{CalculatedX}`→기본값, choose 단·복수).
  - **잔여 14개**: "X장"처럼 고정 숫자가 없는 스케일링·계산형 카드 → ◆가 자연스러운 경우. (원하면 개별 처리 가능)
- ☑ **#3 네크로바인더 시작 덱** — 게임 `StartingDeck` 원문 대조. Soul Spark(유령카드) 제거 → 호위·풀어놓기.
  - **보너스**: 리젠트 시작덱도 오류(별똥별 누락) 발견·수정.
- ☑ **#2·#4 카드 이미지 최신화 + 캐릭터별 기본 카드**
  - GDRE로 게임 v0.108.0 카드 아틀라스 재추출 → 타격/수비를 **캐릭터별 그림**으로(5캐릭 × 2 = 10개).
  - "옛날 이미지"의 실체 없음: 데이터·이미지 모두 현재 게임(v0.108.0)과 일치. 그림 없는 18개는 **게임에 존재하지 않는 STS1 잔재 카드**(추출로도 못 채움).

### 화면 / UX
- ☑ **#5 유물 설명 표시** — 유물을 타일로(아이콘+이름+등급+효과설명). 시작유물 🔒고정, 나머지 ✕제거.
- ☑ **#7 카드 타입 배지** (공격/스킬/파워/상태이상/저주)
- ☑ **#8 카드 강화 전/후 수치 표시**
- ☑ **#9 덱 카드 강화 토글 버튼**
- ☑ **#12 카드 클릭 상세 팝업** — 큰 이미지 + 이름 + 배지 + [기본/강화 후] 토글 + 한글 설명.
- ☑ **#6 보상 입력영역 전체목록 안 펼침** — 이미 그렇게 동작(그리드는 버튼으로만 열림).
- ☑ **한글 초성 검색** (ㅍㅅ→포식). 공식명·추정명 둘 다 대조.
- ☑ **오른쪽 유물 줄 + 추가 칸**

### 도구 / 인프라
- ☑ **#13 git-credential 오류** — 윈도우 자격증명 관리자(VaultSvc) 서비스를 "자동"으로 전환해 해결.
- ☑ 로컬 프리뷰 서버(`serve.js`) + 추출 파이프라인(`tools/`) 정비.

---

## 🟡 부분 완료 (아키텍처 한계)

- 🟡 **#11 자동업데이트 감시**
  - **작동 확인됨**: `check_update.js`가 게임 버전을 감지(현재 v0.108.0 = 추출본 일치 → "최신"). 패치 시 한글 데이터 재추출도 구현됨.
  - **한계**: 브라우저 웹앱은 스스로 게임 파일을 읽거나 백그라운드로 재추출할 수 없음(설계서 §2.3이 명시 — 진짜 자동은 **Tauri(.exe) 단계** 필요).
  - **지금 쓰는 법**: 게임 패치 후 `node tools/check_update.js` 한 번 실행(또는 윈도우 작업 스케줄러 등록)하면 최신 유지.
  - **다음 단계**: Tauri 패키징 시 백그라운드 자동 감시로 승격.

---

## 🎨 추가로 다듬을 것 (선택)

- ☐ **#14 디자인** — 핵심(중립 near-black 팔레트 + 앰버 포인트 + Pretendard 폰트)은 적용 완료.
  더 다듬을 여지: 앰버를 "화면당 2~3곳"까지 더 줄이기, 화면별 여백 미세 조정.
- ☐ **카드 뽑기 → 초기화 동작** — "뽑기" 버튼은 있음. 실제 초기화 흐름 눈으로 최종 확인 권장.
- ☐ 남은 ◆ 14개(스케일링 카드) 개별 문구 처리 — 필요 시.
- ☐ PWA 설치형 → 이후 Tauri(.exe)로 백그라운드 자동감시.

---

## 🆕 새 요청 (2026-07-04 추가)

### #15 강화 시 특수 효과 전수 반영
- 지금은 "수치 증가형" 강화만 반영됨. **수치 없이 특수하게 바뀌는 강화**가 누락됨. 전 카드 검토해서 반영 필요:
  - **코스트 감소** (예: 2코 → 1코)
  - **선천성(Innate) 부여**
  - **소멸/유지 등 키워드 부여**
  - **타격 횟수 증가**, 기타 조건/효과 부여형
- **Claude Code에:** "강화 시 수치만 오르는 게 아니라 특수하게 바뀌는 카드들이 반영이 안 돼 있어. 강화하면 코스트가 줄거나, 선천성이 붙거나, 소멸/유지 같은 키워드가 부여되는 카드를 전부 게임 데이터에서 검토해서 강화 표시·수치·팝업에 정확히 반영해줘."
- ☑ **완료** — 코스트 감소(57장)·키워드 부여(선천성/소멸/유지/공허 28장) 파싱, 팝업 강화후에 "강화 효과: 비용 2→1 · +선천성" 요약. (커밋 cd75cda)

### #16 보유 유물 칸 2열로
- 우측 상단 보유 유물이 한 줄씩 세로로 쌓여 길어짐 → **2열 그리드**로 바꿔 늘어나도 깔끔하게.
- **Claude Code에:** "우측 상단 보유 유물 목록을 한 줄씩 말고 2열로 나눠서 보여줘. 유물이 늘어나도 2열로 채워지게."
- ☑ **완료** — 유물 목록 2열 그리드. (커밋 cd75cda)

### #17 코스트 표시 강조
- 카드 코스트가 눈에 잘 안 띔 → **카드 좌상단에 원형 배경 + 큰 숫자**로 고정(게임 원본 느낌).
- 강화로 코스트가 줄면(#15 연동) 그 변화도 이 코스트 표시에 반영.
- **Claude Code에:** "카드 코스트를 카드 좌상단 모서리에 원형 배경 + 큰 숫자로 고정해서 눈에 띄게 해줘. 게임 원본처럼. 강화로 코스트가 줄어드는 카드는 그 값도 여기 반영돼야 해."
- ☑ **완료** — 카드 좌상단 원형 코스트 배지(.cost-cnr), 강화 감소 시 초록. (커밋 cd75cda)

### #18 그리드 검색 목록 비율·열 수정
- 그리드 검색의 카드가 **가로로 찌그러져** 그림이 뭉개짐(한 줄 2개라 칸이 너무 넓음).
- **카드 원래 비율(세로형)로 복구 + 한 줄 4개**로.
- **Claude Code에:** "그리드 검색 카드 목록이 가로로 찌그러져서 그림이 뭉개져. 카드 원래 세로 비율로 되돌리고, 한 줄에 4개씩 들어오게 해줘."
- ☑ **완료** — 그리드 전용 `.gtop`+`.gimg` 구조로 왜곡 수정, 4열. (커밋 828a829/7b1cba1)

### #19 현재 덱 카드 크기·열 수정
- 현재 덱 카드가 너무 큼 → **그림을 작게, 한 줄에 5개**로.
- **Claude Code에:** "현재 덱 카드가 너무 커. 그림을 좀 작게 줄이고 한 줄에 5개씩 배치해줘."
- ☑ **완료** — 덱 5열, 셀·배지 축소. (커밋 828a829)

### #20 X 코스트가 0으로 잘못 표기
- X 코스트 카드(가진 에너지 전부 소모)가 **0코스트로 표시됨.** 게임 데이터에서 X 코스트는 음수 등 특수 값으로 저장 → 앱이 0으로 떨어뜨린 것.
- ◆ 숫자·특수 강화(#15)와 같은 뿌리(정상 숫자 아닌 특수 값 처리 문제).
- **해결:** 코스트가 특수 값(음수 등)이면 숫자 대신 **"X"로 표시.** #17 코스트 표시와 연동.
- **Claude Code에:** "X 코스트 카드가 0코스트로 잘못 나와. 게임 데이터에서 X 코스트(음수 등 특수 값)를 인식해서 코스트 표시에 숫자 0이 아니라 'X'로 나오게 고쳐줘."
- ☑ **완료** — `HasEnergyCostX` 플래그로 X코스트 10장 감지→"X" 표시, 저주(-1)는 배지 숨김. (커밋 0899af7)

### #21 강화 추천 카드 초록색 강조
- 현재 덱에서 **강화하면 좋은 카드**를 눈에 띄게: **초록 테두리 + 초록 "강화 추천" 배지** 둘 다.
- 디자인 규칙 참고: 초록은 긍정 신호로 절제해서(강화 추천에만) 사용. 앰버(--accent)와 겹치지 않게 구분.
- **Claude Code에:** "현재 덱에서 강화 추천 카드를 초록색으로 눈에 띄게 해줘. 카드 테두리를 초록으로 두르고, 초록 '강화 추천' 배지도 같이 달아줘."
- ☑ **완료** — 다음 강화 1순위에 초록 테두리+초록 ⬆ 배지, 후보는 앰버로 구분. (커밋 0899af7)

### #22 파이어폭스 호환성 (그리드 깨짐)
- 크롬/엣지에선 깔끔한데 **파이어폭스에선 레이아웃이 깔끔하지 않고**, 그리드를 누르면 **카드가 일부만(덜) 나옴.**
- 브라우저 호환성 버그. (CSS grid/flex, 스크롤·높이 계산, 또는 특정 API 차이 가능)
- **Claude Code에:** "파이어폭스에서 레이아웃이 크롬/엣지처럼 안 깔끔하고, 그리드를 열면 카드가 일부만 나와. 파이어폭스에서 재현해서 원인(CSS grid/flex, 높이·스크롤 계산, 브라우저별 차이)을 찾아 고쳐줘. 세 브라우저에서 동일하게 보이게."
- ☑ **해결(#23 슬롯 재구성으로)** — 파이어폭스에서 그리드 안 aspect-ratio가 높이 붕괴하던 문제. 1차로 고정높이 시도(e63338f) 후, **#23에서 인라인 그리드 자체를 슬롯+검색모달로 갈아엎으면서 원인을 제거**함. 모달 그리드는 `grid-auto-rows` 고정 행높이 + 고정 이미지높이라 크롬/엣지/파폭 동일하게 렌더. (카드 검색 모달도 래퍼 구조로)

---

## 🆕 설계 반영 / 추가 작업 (2026-07-05)

### #23 카드 입력을 슬롯형으로 재구성 (설계 §1)
- 기존 "텍스트 입력 + 인라인 그리드" 방식(파이어폭스에서 깨지던)을 제로베이스로 갈아엎음.
- **보상 카드 = 3칸 / 상점 구매 = 7칸** 고정 슬롯. 슬롯 클릭 → **카드 검색 모달**(초성 검색 + 필터 탭 현재 캐릭터/중립/다른 직업/전체 + 카드 그리드). 고르면 슬롯 채우고 **자동으로 다음 슬롯**. ✕로 제거, 클릭으로 교체.
- 모달 그리드는 `grid-auto-rows` 고정 행높이 + 고정 이미지높이 → 크롬/엣지/파폭 동일 렌더(#22 해결).
- ☑ **완료** (커밋 8e00c2c)

### #24 STS1 잔재 카드 제거 + 누락 카드 추출
- **STS1 잔재 19장 제거**(게임 C#에 없음): 아이언클래드 사신·약점포착·두 번 치기·Heavy Blade 등 17 + Follow Through(사일런트) + Child of Stars(리젠트).
- **게임엔 있는데 DB에 없던 카드 중 그림 있는 3장 추가**: 공허의 부름(Call of the Void)·Scare·Tank (그림+스탯+한글설명, 카드풀 확인).
- 그림 없는 15장(심야·조제·칼날교향곡 등)은 **코드에만 남은 베타/삭제 카드**로 판단해 제외(그림 유무로 실제 카드 판별).
- `data/extra_cards.js`로 런타임 보강 → **db.js 원본 무수정**. 철자무시 매칭으로 이름차이 오판 방지.
- ☑ **완료** (커밋 9527e3e)

### #25 카드 이름 크기 확대
- 슬롯·검색모달의 카드 이름 폰트를 키워 가시성 개선(.62→.74rem).
- ☑ **완료**

### #26 런 복기 / 실패 분석 메뉴 (설계 §6)
- 상단 메뉴 탭으로 **어드바이저 ↔ 런 복기** 전환. 런 선택 시 규칙 기반 한글 분석 4종: **덱 구성 · 사망 지점(사망 적/막·HP 급감·막 진행) · 카드 선택 복기(픽/스킵·놓친 고평가 카드) · 유물 활용도**. 카드/유물 ID를 DB로 매핑해 한글 표시. "패배한 런만" 필터.
- ☑ **완료** (커밋 fd313e3)

### #27 런 기록 자동 로드
- 폴더 수동 선택이 번거로워, `tools/build_run_history.js`로 `saves\history`를 슬림 추출 → `data/runs.js`(232런) → **앱 열자마자 자동 로드**. file://에서도 바로 보임.
- ⚠️ 스냅샷이라 새 런은 `build_run_history.js` 재실행(또는 화면의 "최신 폴더 갱신")으로 갱신.
- ☑ **완료** (커밋 0440116)

---

## 💡 나중에 생각해볼 것

- 런 복기 고도화: 반복 사망 패턴, 캐릭터별 승률 통계, 런 목록 정렬/검색.
- 완전 자동 데이터/런 갱신 → Tauri(.exe) 단계.
- (여기에 떠오르는 아이디어 계속 추가)
