# Arcana Codex — 확인 & 수정 목록

## 다음 세션 재개 지점 (2026-08-19 기준)

> 이 문서만 읽고도 상황 파악이 되도록 배경과 근거를 함께 적는다.
> 새 대화창을 열면 이 섹션부터 읽을 것.
> 채팅창을 여러 개 띄워 작업하므로, "했나 안 했나"가 헷갈리면
> 기억이나 보고를 믿지 말고 각 항목의 **확인법**을 실행할 것.
> Claude Code에 물을 때는 "~했나요?"가 아니라
> "`git log --oneline -5` 결과를 보여줘"처럼 실제 명령 결과를 요구할 것.

### 현재 상태
- 스냅샷: 게임 v0.111.0 기준 카드/통계, 런 기록 314개(승 72·패 183·포기 59)
- 승천10(A10) 통계 전 캐릭터 보유 (2026-08-19 재빌드에서 신규 수신 — 아래 8/19 완료 참조)
- 런 스냅샷에 rooms(room_type, turns_taken)와 damage_taken 포함됨 (2.5MB)

### 2026-08-19 완료 (확인법 포함)

1. **A10 통계 upstream 회귀 해소** — 이슈 제출했던 ptrlrd/spire-codex#868이
   #874로 수정되어 해결됨. 2026-08-19 재빌드에서 ironclad/regent/necrobinder
   A10 전부 신규 수신 확인.
2. **사일런트·디펙트 A10 값 동결 해소** — 같은 재빌드에서 8/15에 고정돼 있던
   값이 신규 값으로 교체됨(`inherited` 표시 사라짐).
   확인(1·2 공통): https://spire-codex.com/api/runs/stats?character=IronClad&ascension=10
   이 정상 응답 + `data/api_data.js`의 statsHi에 5캐릭터 모두 존재·inherited 없음
3. **ABUNDANCE / DOWSING 티어 누락 — 원인 규명 후 종결**
   - upstream 버그 아님: 채점 대상 필터 정규화로, 원래 채점 대상이 아니던
     두 장(특수 희귀도 Ancient·Quest)이 정리된 것
   - 등급 전 캐릭터 불변(ABUNDANCE B, DOWSING D) → 사용자 체감 영향 0
   - 둘 다 보상 선택지로 등장하지 않는 카드
   - upstream 이슈 제기 불필요로 종결
   부수 발견: 가드레일 사각지대(아래 다음에 할 일 6번), MULTI_CAST 중복,
   유령 id SCARE (아래 기록만 참조)

### 2026-08-18 완료 (확인법 포함)

1. **푸터 데이터 버전 표시** — 고지문 뒤에 `<span id="dataver">`를 새로 붙이고
   JS가 그 span의 textContent만 채운다. 고지문 innerHTML은 건드리지 않음(PR #35).
   날짜는 `generatedAt.slice(0,10)` — **UTC 기준**. 아래 「기록만」 항목 참조
   부수 변경: 246행 `padding-bottom` 72px → 86px
   (버전 표시로 360px에서 푸터가 5줄=78px이 됨. 320px는 이전부터 이미 넘쳤던 것도 같이 해결)
   확인: 푸터 맨 끝이 `· 데이터 v0.111.0 (2026-08-16)`

2. **승천 필터 A10 부재 안내문** — 필터가 '최고(A10)'인데 그 캐릭터의
   statsHi가 아예 없으면 추천 결과 위에 한 줄 안내(PR #36).
   `renderRec()` 맨 앞, 빈 슬롯 조기 return보다 위에서 토글 — 빈 화면 잔류 방지.
   `off.length` 조건은 **일부러 넣지 않음**: 893행 캐릭터 전환이
   `clearAllOffered()`로 슬롯을 비우므로, "필터 켠 채 캐릭터 전환 → 빈 슬롯"이
   실제로 자주 생긴다. 그 순간 바로 알리는 게 의도.
   확인: 아이언클래드 + 최고(A10) → 안내문 표시 / 사일런트에선 미표시

3. **upstream에 API 장애 이슈 제출** — ptrlrd/spire-codex#868
   https://github.com/ptrlrd/spire-codex/issues/868

### 2026-08-16 완료 (확인법 포함)

친구 피드백 대응 (코드):
1. **보상/상점 탭 카드 목록 분리** — 탭이 목록을 공유하던 문제.
   상한을 하드코딩 8에서 slotCount()로 바꿔 숨은 초과분이 생기지 않게 함
   확인: 보상 탭에 3장 넣고 상점 탭 갔다 돌아오면 3장 그대로
2. **런 기록 폴더 안내 정확화** (프로필명 폴더 누락 + AppData 숨김 폴더 문제)
   확인: 런 복기 왼쪽 패널에 "폴더를 못 찾겠어요" 접기가 있음
3. **막 번호 계산 수정** — r.acts는 "예정 경로"라 1막에서 죽어도 항상 3이었음.
   map_point_history의 실제 그룹 수로 교체. **패배 런 124건 중 101건(81%)이 틀려 있었음**
   확인: 1막에서 죽은 런을 선택 → "1막에서 패배"로 표시
4. **사망 분석 전면 교체** — 기존 "체력 A→B 급감" 판정은 사망 시 HP가 항상 0이라
   사실상 "마지막 전투에 25 이상으로 들어갔나"만 묻던 무의미한 판정이었음.
   실측 183건 분포 기반 3층 구조로 교체 (index.html의 DEATH_TH 상수)
   확인: 패배 런 선택 → "마지막 전투(…)를 체력 N으로 시작해 …" 줄이 나옴

그 외:
- **OG 링크 미리보기** — 메타 태그 15줄 + `og-image.png`(1200×630)
  확인: `https://thecaous1-hash.github.io/arcana-codex/og-image.png` 열기 → 그림 뜸
  ⚠️ 미리보기는 캐시됨. 이미지 교체 시 `og-image-v2.png`로 새 이름 쓸 것
- **골골이 명칭 통일** — `i18n.js` 2곳만 (공식 번역이 "골골이")
  내부 키(`osty`, `osty_atk` 등 db.js 233곳)와 `{OstyDamage}` 자리표시자는 불변
  확인: 네크로바인더 선택 → 태그가 "골골이/소환"
- **모바일 레이아웃 3건** — 기존 `@media(max-width:420px)` 안에만 추가
  피드백 버튼 44×44 원형 / `.rec-name` `keep-all` / `.reason` `keep-all` + `.nb` nowrap
  확인: 폰에서 카드 이름 안 쪼개짐, 승률 줄 자연스러움
- **version 필드 구조 수정 ★** — 아래 별도 항목 참조
- **현재상태.md에 「깃 작업 절차」 섹션 추가**

### 사망 분석 설계 요약 (수정 시 참고)
- 문턱은 index.html의 DEATH_TH 한 곳에 모여 있음. 조정은 여기만 고치면 됨
- 실측 분포(패배 183건): 진입체력 중앙 36 / 소요턴 중앙 6 / 턴당피해 중앙 5.5, 90% 9.1
- 사망 방: 엘리트 76 · 보스 74 · 일반 33 (엘리트가 보스보다 많음)
- 2층은 최대 2개, 우선순위 A>B>E>C>D. A 발동 시 B 억제(같은 현상 중복 서술)
- 예외: 진입체력 - damage_taken >= 1이면 기록을 못 믿으므로 2층 전체 침묵 (183건 중 8건)

### ★ version 필드의 정체 (2026-08-16 규명)

기존 `version`은 카드 데이터 버전이 아니라 **최근 런 20개 중 최빈 build_id**였다.
같은 날 몇 시간 사이 v0.103.2 ↔ v0.107.1로 출렁였다.
모르고 푸터에 붙였으면 갱신할 때마다 버전이 오르내리는 사이트가 될 뻔했다.

`tools/build_from_api.js` 수정:
- `version` → `betaDiff.version` 기준 (v0.111.0)
- `sampleBuildId` 신설 — 옛 build_id 값을 참고용으로 보존
- 헤더 라벨 `게임 vX` → `데이터 vX` ("게임"이 오해의 근원)
- betaDiff 실패 시 `keep()`이 이전 값 물려받음 → 헤더에 `(물려받음)` 표시
- 물려받을 값도 없으면 `null`. 옛 build_id로 폴백하지 않음(혼동 재생산 방지)

확인: `data/api_data.js` 2행이
`// 소스: spire-codex.com/api · 데이터 v0.111.0 · 생성 2026-08-16`

### 다음에 할 일 (우선순위 순)

## 2026-08-23 디시 첫 공개 — 실사용자 피드백 기반 최우선 과제

디시 슬레이더스파이어 마이너 갤러리 업로드(20:20, 정보/ 말머리).
20분 시점 조회 110 / 댓글 4 / 추천 3 — 해당 페이지 조회수 1위.
아래 A~C는 전부 실사용자가 직접 짚은 문제이며,
등급이 틀린 것보다 "근거가 자기모순"인 쪽이 더 치명적이므로
기존 배지·가중치 작업보다 우선함.

※ 8/24 기준 A·B 완료. 남은 것은 C(빌드 적합 판정), D(런 복기 화면),
  F(용어 불일치), G(인챈트·빌드 인식 사각지대), H(빌드 부적합 감점 누락).


**A. syn/anti 태그 중복 10장 — ✅ 완료 (8/24)**

증상: 같은 빌드에 대해 가점과 감점이 동시에 표시됨
  예) 분노(ANGER): "+0.3 소진 빌드에 적합" 과
                   "-0.9 소진 빌드와 충돌"이 나란히 출력

원인: db.js에서 syn 배열과 anti 배열에 같은 태그가 중복 등재됨.
logic.js의 시너지 루프(약 176행)와 안티시너지 루프(약 193행)가
각각 독립적으로 순회하므로 상호 배제 장치가 없음.

해당 카드 10장 (중복 태그):
  ANGER (exhaust), DOMINATE (exhaust), STOKE (self_damage),
  BURST (shiv), STORM_OF_STEEL (sly), DECISIONS_DECISIONS (forge),
  NEUROSURGE (doom), DEVOUR_LIFE (osty), RATTLE (osty),
  FLATTEN (soul)

처방 (두 가지 다 필요):
  1) 데이터 수정 — 10장 각각 syn/anti 중 어느 쪽이 맞는지 판단해
     한쪽에서 제거. 카드별 판단 필요하므로 일괄 처리 금지
  2) 로직 가드 — 같은 태그가 syn·anti 양쪽에 있으면 둘 다 적용하지
     않도록 방어 코드 추가. 향후 데이터 실수 재발 방지용

✅ 완료 (8/24, 커밋 162a285 + b006f50, PR #44)
- 데이터 10장 수정: 9장은 anti에서 제거, ANGER만 syn에서 제거,
  STOKE는 self_damage를 양쪽에서 제거
- 로직 가드: syn·anti 양쪽에 있는 태그는 가점·감점·근거 표시
  전부 제외 (상쇄나 한쪽 우선 적용 안 함)
- 추가 수정: 역할 밸런스 가점을 topArch 소속 카드로 제한.
  분노가 "빌드와 안 맞음" 감점과 "자원 생성 기여" 가점을
  동시에 받던 별도 경로 차단
- 검증: syn/anti 중복 0장, verify_blend 통과


**B. 유물 보너스 재검토 — ✅ 완료 (8/24)**

B-1. 시작 유물 scoreEffects 삭제
  근거: 시작 유물은 해당 캐릭터 전원이 100% 보유.
  승률 통계와 전문가 티어가 이미 그 유물이 있는 상태를 평가한 값이므로,
  추가 가산은 이중 계산. 게다가 화면에는 "◈ 유물이 강화" 형태로
  떠서 사용자가 '발견된 시너지'로 오독함.

  현황:
    불타는 혈액(아이언클래드) — scoreEffects 없음, 영향 0
    갈라진 핵(디펙트) — scoreEffects 없음, 영향 0
    뱀의 반지(사일런트) — sly/draw +0.2
    신성한 권리(리젠트) — stars/star_gain +0.5
    속박된 성물함(네크로바인더) — osty/soul/doom +0.3

  ※ 강화판(검은 피, 주입된 핵, 드레이크의 반지, 신성한 운명,
     해방된 성물함)은 획득 여부가 갈리므로 삭제 대상 아님. 유지.

B-2. relicCombos 상한 검토 (영향이 훨씬 큼)
  DB.relicCombos 146개 항목, 최대 +2.5.
  +2.5는 등급 두 칸 이동에 해당(C → S 가능).

  최대치 예시:
    Strike Dummy + Perfected Strike +2.5
    Ninja Scroll + Accuracy +2.5
    Funerary Mask + Haunt +2.5
    Power Cell + Claw +2.5

  ⚠ 구조 문제: 전문가 앵커 클램프는 3원 블렌드 단계에서 걸리는데
  유물 보너스는 그 이후에 가산되므로 가드레일 바깥임.
  → 유물 가산도 클램프 안으로 넣을지, 별도 상한을 둘지 결정 필요

  실사용자 지적: "거상이 소뿔모양 걸이 유물 있다고 매우 높은
  추가 점수를 받는다"

✅ B-1 완료 (8/24, 커밋 080edeb, PR #46)
시작 유물 3종 scoreEffects 삭제 (뱀의 반지/신성한 권리/속박된 성물함).
강화판 5종은 유지. 등급 변경 다수 확인(전부 하락 방향).

✅ B-2 완료 (8/24, 커밋 d03f841, PR #47)
relicCombos 가산에 감쇠 적용. 내림차순 정렬 후
DIMN[1.0, 0.6, 0.3], 4번째 이후 0. 발톱 +12.3 → +4.3.
콤보 1개 카드는 무변화 확인.

✅ B-3 완료 (8/24, 커밋 cedfaff, PR #48)
유물 scoreEffects 이상치 3종 축소:
  ANCHOR 5 → 0.5, HORN_CLEAT 5 → 0.5, PERMAFROST 5 → 0.4
원본 저장소(Shawnrai/Shunrai-s-STS2-Advisor)에도 5로 되어 있어
포크 시점부터 있던 값. 소수점 누락 추정(원본에서 0.5가 39개로 최빈값).
Joss Paper + Blade Dance 콤보 중복 제거(146 → 145개).

효과: 소뿔모양 걸이 하나로 기본 Defend가 4.88(S) → 0.08(D)로 정상화.
유물 하나가 카드를 부당하게 S로 밀어올리던 왜곡 해소.

⚠️ 미해결: 거상(Colossus)은 수정 후에도 6.0 상한에 닿음
(기본 점수 5.30 + 유물 0.8 = 6.10 → 절사).
유물 기여분은 정상화됐으나 상한 포화 자체는 남음.
→ 6.0 상한 구조는 별도 검토 대상


**C. 빌드 적합 판정이 너무 헐거움**

시너지 판정이 arch.core 뿐 아니라 arch.support까지 인정하는데,
support 태그가 너무 범용적임.
  예) exhaust 아키타입 support = draw, block, energy_gain
  → 방어 카드면 사실상 대부분 "소진 빌드에 적합" 판정을 받음

실사용자 지적 2건이 여기서 파생된 것으로 추정:
  - "어포·무갑 다 있는 상태에서 자해 빌드가 힘 빌드와 충돌이라고
     화력 증폭 점수가 깎임"

처방 후보: support 매칭 시 가중치 축소, 또는 core 매칭이 있을 때만
support를 인정, 또는 support 태그 목록 자체를 좁힘. 판단 필요.

추가 사례(8/24, A항목 작업 중 발견):

- 부패(CORRUPTION)가 소진 덱에서 "+0.3 Exhaust 빌드에 적합"과
  "-0.9 Exhaust 빌드와 충돌 (block)"을 동시 표시.
  소진 아키타입 support에 block이 있어서, anti:[block]인 카드가
  소진 빌드와 충돌한다고 판정됨. C항목의 전형적 사례.
  ※ 부패는 소진 빌드의 대표 엔진 카드인데 "충돌" 표시가 뜸

- 역할 가점 게이트(b006f50) 적용 시 네크로바인더 카드 4장이
  −0.6~−0.9로 변화폭 상위에 집중
  (Pagestorm, Borrowed Time, Seance, Capture Spirit).
  정정(8/28): "8/23 발견한 anti 48장과 같은 구역"이라는 추정은
  절반만 맞음 — 실제로는 4장 중 2장만 포함.
  공통점은 anti가 아니라 전원 role:"engine" + builds에 soul.
  → 원인은 역할 가점 게이트(b006f50) 쪽

- 참고: A항목 syn/anti 가드는 태그 배열만 막음.
  role 기반 경로는 b006f50에서 별도로 게이트 적용됨.
  아직 남은 모순 경로는 위 부패 사례(anti 태그 ↔ arch.support)

✅ 조사 완료 (2026-08-25, 커밋 52cad29) → docs/조사_C_빌드적합판정.md
   모순 111건(고유 카드 90장). 부패형은 7건뿐, 나머지 104건은
   역방향(가점=support, 감점=core).
   진범은 scaling — 16개 아키타입 중 11개의 support,
   역방향 104건 중 68건이 scaling 경로.
   구조 원인: logic.js:185에서 core와 support가 ||로 동급,
   채점 가중치도 동일. core/support 구분은 감지 단계에만 존재.
✅ 처방 결정: ② core 우선 (2026-08-28)
   근거: docs/조사_C2_처방시뮬레이션.md (커밋 d6af840)
   - ③은 모순 해소 0건에 점수변화 1021쌍 → 탈락
   - ①은 모순 111건 해소·등급변동 6장이나 부패형 7장이 "적합"도 잃음
   - ②는 모순 111건 해소, 부패형은 가점만 남고 역방향은 감점만 남음
     등급변동 66쌍(↑6/↓60)
   ⏳ 선행 과제 2건이 해결돼야 구현 가능 (아래 D·E 항목)
   별도 판단: scaling을 support 목록에서 어떻게 할지 (데이터 결정)


**Dirge 자기모순 잔존** — builds=[soul,osty] / anti=[doom,osty].
A항목(162a285)이 5장 중 4장만 정리하고 누락.
A 가드는 syn↔anti만 막아 builds↔anti는 통과.
→ 조사 E(8/29)에서 같은 유형 1장 추가 발견(잉크 칼날) — 2장짜리 수정으로 확대.


**네크로 anti 일괄생성 확정** (8/28)
87장 중 48장의 anti가 아키타입 id로만 구성, 45장은 정확히
"전체 아키타입 − 자기 builds" 공식. 사용자 판정 결과:
  - doom↔soul: 비충돌 → 딱지 삭제 대상
  - doom↔osty, soul↔osty: 실제 충돌 → 유지
  - 공격명령(Sic 'Em): 규칙대로 D 수긍 (예외 처리 안 함)
미리보기: 딱지 44개 삭제(34장) 시 모순 111→95, ②등급변동 66→56쌍
Dirge 자기모순(builds↔anti의 osty)은 이 규칙에 포함되어 자동 해결


**조사 E 완료 — 전 캐릭터 형제 아키타입 anti** (8/29, 커밋 0cad1fd)
→ docs/조사_E_형제아키타입anti.md
  - 공식 일괄생성("전체 아키타입 − 자기 builds")은 3캐릭터에 집중:
    리젠트 44장 · 네크로 45장 · 사일런트 25장.
    아이언클래드 1장, 디펙트 0장 → 이 둘의 anti는 수작업이라 신뢰 가능
  - 아키타입 쌍 20개 중 딱지 0인 쌍 3개. 판정 질문지 17문항 (보고서 말미)
  - 자기모순 카드 2장: Dirge(anti의 osty가 core에 없어 무발화)
    + **잉크 칼날/Blade Of Ink(silent)** — builds:[shiv,sly] / anti:[poison,sly]로
    sly가 겹치고, sly가 core에 있어 "+0.8 빌드 핵심"과 "−0.9 교활 빌드와 충돌"이
    같은 채점에 동시 출력됨(실측 확인, 기준선 5.02 S). **우선 수정 대상**

**사용자 쌍 판정 결과** (8/29)
  - 리젠트 별↔단조: **실제 충돌** → 딱지 44장 유지 (최대 규모 쌍 확정)
  - 사일런트 교활↔비수, 교활↔중독: **쌍 단위 판정 불가**
    ("카드마다 시너지가 있기도 없기도 함") → 카드별 판정 필요
  - 네크로 3쌍은 기판정 유지 (doom↔soul 비충돌 / osty 쌍 충돌)


**any 태그 정합성 조사 완료** (8/28, 커밋 47d3474)
→ docs/조사_D_any태그정합성.md
  - any는 빌드 정보가 아니라 사실상 "좋은 카드" 표식
    (S티어 58%에 부착, D티어는 11%)
  - 부여 기준·시점·주체 확인 불가. 저장소 최초 커밋부터 865개 불변
  - 제외형 6곳 / 통과권형 9곳으로 용법이 갈려, 같은 채점에서
    misfit 감점(any 제외)과 역할·2막 가점(any 인정)이 동시 발화 211쌍
  - anti 충돌 감점과 any 가점 동시 발화 43쌍
  - any 제거 시 등급변동 69장(전부 하락). 경로는 logic.js:440-441(2막 적합 +0.2)과
    logic.js:341 게이트(첫 엔진 +0.3) 두 곳
  ⏳ 처방 미결정: 데이터 204장에서 제거 vs logic.js 두 곳만 수정

**any 처방 검증** (8/29)
  - 코드 2줄 수정으로 데이터 204장 제거와 동일 효과 확인:
    logic.js의 cardFitsTopArch(341행)와 fitsTop(440행)에서 ||'any' 제거
  - 실측: 점수변화 1284쌍 / 등급변동 227쌍(고유 69장) 전부 하락
    S→A 97 · A→B 53 · B→C 53 · C→D 24
  - ⏳ 검수 보류 — H항목(빌드 부적합 감점 누락) 때문에 이 상태가
    최종 등급이 아님


**D. 런 복기 초기 화면 오해 소지**

실사용자 지적: "런복기 누르니 남의 런 뜨는 듯, 그건 고치는 게 나을 듯"
파일을 넣지 않은 상태에서 예시 데이터가 표시되는 것으로 보이며,
처음 온 사용자는 자기 데이터 오류로 오해함.
→ "예시 데이터입니다" 명시 또는 빈 상태 안내로 변경


**E. 기존 과제 — 우선순위 하향**

기존 1번(현 2번, 배지 + DELTA 가중치)과 v0.108~0.110 패치노트 대조
(TRANSFIGURE, MANGLE, CRIMSON_MANTLE, COLOSSUS)는 위 A~D 이후로 미룸.
다음 패치까지 최소 한 달 여유가 있어 급하지 않음.


**F. UI 용어가 게임 공식 한글 번역과 불일치**

발견(8/24): i18n.js가 doom을 "파멸"로 표기하나,
게임 공식 한글은 "종말"임.
근거: data/api_data.js 카드 설명에 "종말" 34회 등장하며
[gold] 키워드 태그로 강조되어 있음. "파멸"은 0회.
  예) 도망칠 수 없다 — "[gold]종말[/gold]을 10 부여하고…"
      때가 되었다   — "대상 적이 보유한 [gold]종말[/gold]만큼…"
      망각         — "대상 적에게 [gold]종말[/gold]을 3 부여합니다"

수정 대상 (확인된 것):
  i18n.js  doom: "파멸"        → "종말"
  i18n.js  "Doom Stack": "파멸 중첩" → "종말 중첩"
  ※ 원문이 Doom Stack이므로 "종말 중첩"이 직역

⚠️ 주의: 카드 이름 End of Days는 "종말의 날"로 번역되어 있음.
키워드 "종말"과 카드명 "종말의 날"이 유사하므로
UI에서 혼동되지 않는지 확인 필요.

⚠️ 하나가 어긋났다는 것은 전수 대조가 안 됐다는 뜻.
i18n.js의 태그·아키타입·상태이상 한글 표기 전체를
api_data.js의 공식 번역과 대조해야 함.
(작업 시 조사 먼저, 수정은 그다음)

영향: 사용자가 게임 용어와 앱 용어를 연결하지 못함.
특히 네크로바인더는 자원이 3종(종말·영혼·오스티)이라 혼란이 큼.
디시에서 제보를 받을 때도 용어가 어긋나면 소통 오류 발생.
제작자 본인도 혼동한 사례가 있어 신규 사용자는 더 심할 것.

범위 확대 (8/29 발견)
i18n.js의 카드 한글명이 api_data.js(게임 공식)와 불일치하는 사례 다수.
  Panache 멋/위풍당당 · Supercritical 초임계/임계 초과
  Modded 개조됨/모드 적용 · Skim 훑어보기/걷어내기
현재 화면 표시는 정상 — index.html:743-744가 API 이름을 우선하고
i18n.js는 폴백으로만 사용.
다만 API 스냅샷에 없는 카드나 폴백 전환 시 틀린 이름이 노출될 위험.
영어화 작업 시 i18n.js를 손대게 되므로 그때 함께 정리.


**G. 인챈트·빌드 인식 사각지대** (8/28 발견, 사용자 A9 클리어 경험)
  - ENCH_SCORE_FX에 인챈트 1종(CORRUPTED)만 등록. 나머지 21종 점수 미반영
  - 총합 캡 ±0.6 — 판을 뒤집는 인챈트를 표현하기엔 작음
  - 구조 한계: "덱에 인챈트 있음 → 태그 일괄 가산" 방식이라
    "어느 카드에 붙었나"를 못 씀. SLITHER(스르륵)는 붙는 카드
    코스트에 따라 가치가 정반대 → 현 구조로 표현 불가
  - 매장(bury)도 builds에 1장(빌린 시간)뿐이라 빌드로 감지 안 됨
  - 네크로 티어 분포: soul 43% / osty 38% / doom 23% (S+A 비율)
  → "실력자 픽 같지 않다" 피드백의 유력 원인 후보. Act1 가중치와 별개 사안


**H. 빌드 부적합 감점 누락** (신규, 8/29 발견)
사용자 지적: 위풍당당(Panache, colorless, S)이 힘 덱에서 any 제거 후에도
A로만 내려감. 그러나 이 카드는 "턴당 카드 다수 사용" 조건 카드라
힘 덱에서는 훨씬 낮아야 함.
원인: builds=[claw,sly,any]인데 **anti가 비어 있음** → 안 맞는 빌드에서
감점 경로가 아예 없어 기본 티어값이 그대로 남음.
notes에는 "in Claw or Sly builds playing 10+ cards per turn"이라고
조건이 명시돼 있으나 점수 계산에 미반영.
→ 지금까지 다룬 C·D는 "부당한 가점 제거"였고 이건 "필요한 감점 부재".
   방향이 반대이며 별도 조사 필요.
✅ 조사 완료 (2026-08-29, 커밋 556a3e9) → docs/조사_F_anti공백.md
   - 591장 중 anti 빈 카드 430장(72.8%). 무색은 126/126으로 100%
   - 구조 사실: anti가 비어도 misfit 감점(logic.js:311-317, −0.9)은 받음.
     비는 것은 anti 충돌 감점(logic.js:200-210)이라 최대 −1.8이 −0.9에서 멈춤
     → "감점이 없다"가 아니라 "감점이 절반"
   - builds 구체적 + anti 공백인 S·A 티어 66장(S 14 + A 52)
   - 실측: 66장 × 비소속 아키 187행 중 35행이 감점 후에도 S 유지
   - notes에 조건이 있으나 태그화 안 된 카드 10장
   - 무색 anti 공백이 의도인지 미작업인지는 확인 못 함
   - 부수 발견: 싸움 준비(Expect a Fight)의 anti에 `exhaust_heavy`가 있으나
     이 태그는 어느 아키타입에도 없어 영원히 발동 불가 (죽은 태그)
   - 반대 사례: DB.combos로 참호↔바리케이드, 부식↔쇠락이 이미 연결돼 있어
     "카드 간 관계 표현 장치"는 이미 존재함
   ⏳ 처방 결정: A안(S 14장만 우선 처리) 채택. A급 52장은 후순위

**사용자 검수 기준 확정** (8/29): "방향이 맞나"가 아니라 "최종 등급이 맞나"
→ H항목이 해결되기 전에는 등급 검수를 진행하지 않음


**1·2단계 수정 완료 (미머지) — 최종 검수 결과 반영 대기**
- 1단계 데이터 정리: main에 머지 완료 (48d3a5e)
- 2단계 로직 수정: 브랜치 claude/stage2-logic (b2cca14)
  ✅ 3단계 구분안과 함께 PR #64로 main에 머지 완료 (2026-08-30)
  — "2단계 머지 보류" 상태는 해소됨
- B-1 검산 통과: 실제 logic.js와 시뮬 ② 3,448쌍 전수 일치, 불일치 0
- 최종 규모: 등급 변동 292쌍 / 고유 115장 (상승 6, 하락 286)

**사용자 최종 검수 결과 (2026-08-29, S·A 하락 55장 전수)**

하락 부당 9장 — 전부 "빌드 무관 범용 유틸" 성격:
  무색: 숨겨진 보석, 대혼란, 충격파, 영원의 갑옷
  아이언클래드: 연쇄, 제물
  사일런트: 귀를 찢는 비명, 발놀림, 탈출구, 들춰내기
  → 1단계 때 사혈·거상을 제외한 것과 같은 유형

더 내려가야 함 4장 — 기본 티어가 실제보다 높음:
  굴러가는 바위, 주먹다짐, 박치기, 전투의 북소리(타격)
  → 티어는 spire-codex 승률 데이터 출처라 앱 로직으로 조정 불가. 별건

판정 보류 5장 (사용자가 안 써본 카드):
  계몽, 탐색 타격, 싸움 준비, 유령의 형상, 악몽

전원 통과: 네크로바인더 6장, 리젠트 5장, 디펙트 5장 (한 장도 안 걸림)

관찰: 사일런트만 11장 중 4장이 부당(36%). 다른 캐릭터는 0%.
조사 E의 "사일런트는 쌍 단위 판정 불가, 카드마다 다름"이 그대로 재현됨.

⏳ 다음 세션 결정: 부당 9장 처리 방법

**남은 모순 2건** (2단계 보고서 §5)
  제물(ironclad/exhaust, syn draw ↔ anti block)
  위풍당당(necrobinder/osty, syn damage ↔ anti block)
  둘 다 1단계에서 새로 단 anti `block`이 만든 support↔support 조합.
  처방 ②의 해소 범위(core↔support) 밖이라 안 잡힘.

**3단계 — any 구분안 + 데이터 예외 (2026-08-30)**
브랜치 claude/stage3-any-onlyfix (claude/stage2-logic에서 분기)

배경: 8/29 최종 검수의 "부당 하락 9장"은 전부 builds가 `any` 단독인 카드였다.
작업 A로 any 통과권을 없애면서 이들만 빌드 정보가 통째로 사라진 것.
조사 D가 "any = 좋은 카드 표식"이라 판정한 것은 any가 구체 빌드와
**섞인** 카드(78장) 얘기였고, any **단독** 카드(130장)는 성격이 다름.

처방(구분안): builds가 any 단독이고 anti가 비어 있으면 통과권 유지.
  - logic.js 2곳(cardFitsTopArch·fitsTop)에 onlyAny 조건 추가
  - anti 조건을 넣은 이유: builds=["any"]인데 anti가 달린 카드 7장에서
    "+0.2 확정된 빌드에 적합"과 "−0.9 ○○ 빌드와 충돌"이 동시 출력됨
    (마지막 축제·격돌·유령의 형상·초강력 광선·공허 형상·공명·잔혹)
데이터 예외 2건:
  - 제물(Offering): 1단계에서 단 anti [strength, block, strike] 전부 삭제
    → 3.10(B)에서 1단계 이전 값 4.80(S)로 복귀. 사혈·거상과 동일 처리
  - 탈출구(Escape Plan): builds ["sly","any"] → ["any"]
    ("any 단독" 조건에 걸리게. any면 sly도 포함이라 손실 없음)

결과(3,445쌍 전수, 기준선 조건 동일):
  main → stage2      등급변동 292쌍 (상승 6 / 하락 286), 모순 1건
  main → 3단계 최종   등급변동 129쌍 (상승 9 / 하락 120), 모순 0건
  부당 지목 9장 전원 원등급 복귀 확인

✅ 2단계 로직 + 3단계 구분안: PR #64로 main에 머지 완료 (2026-08-30)
✅ 8/29 main(fe76e06) 대비 최종 규모 (죽은 anti 태그 삭제 반영):
   등급변동 125쌍 (상승 9 / 하락 116), S·A 하락 42쌍 / 24장,
   모순 0건 (misfit+적합 동시 0, 충돌+적합 동시 0)


**잉크 칼날(Blade Of Ink) 자기모순** — silent, A티어
builds=[shiv, sly] / anti=[poison, sly]로 sly가 양쪽에 있음.
Dirge와 달리 sly가 아키타입 core에 있어 실제로 발화:
"+0.8 빌드 핵심 카드"와 "−0.9 교활 빌드와 충돌"이 같은 채점에 동시 출력
(기준선 5.02 S, 실측 확인). 사용자에게 보이는 상태이므로 우선 수정 대상.
✅ 1단계에서 수정 완료 (ac9606b, main 머지됨) — anti에서 sly 삭제

**죽은 anti 태그 `exhaust_heavy`** — 싸움을 예상하라(구 싸움 준비)
✅ 삭제 완료 (2026-08-30). 죽은 태그는 이것 하나뿐임을 전수 확인.
⏳ 별건 남김: 싸움을 예상하라가 실제로 소진 빌드와 충돌하는지
   판정 필요. 판정 시 anti에 exhaust를 넣으면 소진 빌드에서
   4.44(S) → 2.54(B)로 떨어짐(-1.9). 사용자가 안 써본 카드라 보류 중.
   ※ 4.24는 죽은 태그가 남아 있던 검수 문서 시점 값이라 정정함.
     태그 삭제로 현재 기준값은 4.44(S)임.

**밴시의 외침 builds `[doom, doom]` 중복** — 조사 중 발견, 미수정.

**MULTICAST / MULTI_CAST 카드 id 충돌** (defect) — 조사 중 발견, 미수정.


**2. 배지 + DELTA 가중치 조정** — 8/22 조사 완료, 처방 확정 단계

■ 8/22 조사 결과 (Claude Code, 파일 수정 없음)

betaDiff.changed 78장 분류 (합 78 검증됨):
- A군 63장 = 성능 변경 (damage/block/vars/cost 등 17개 필드)
- B군 10장 = 희귀도·획득경로만 변경 (rarity 9장, sources 1장)
- C군  5장 = 문구만 (HELIX_DRILL, TRACKING, FORGOTTEN_RITUAL,
             ROCKET_PUNCH, BEAT_INTO_SHAPE)
- 판정 불가·규칙 밖 필드 없음

A+B군 73장 중 현재 S/A 등급 = 22장, 멀티 전용(TANK) 제외 시 21장
계산은 verify_blend.js와 동일 경로로 앱 실제 산식 호출

21장 내역:
- S등급 · 일반카드 3장: TRANSFIGURE(necro), MANGLE(ironclad),
  CRIMSON_MANTLE(ironclad)
- S등급 · event 2장: WHISTLE, MAD_SCIENCE (전 캐릭터 S)
- A등급 · 일반카드 11장: SACRIFICE, TAUNT, EXPERTISE, DEMON_FORM,
  ALIGNMENT, EXPECT_A_FIGHT, MIRAGE, COLOSSUS, REND, EIDOLON,
  WELL_LAID_PLANS
- A등급 · event 2장: BRIGHTEST_FLAME, RELAX
- B군 3장: CRUELTY, SALVO, BLOODLETTING (전부 A)

■ 결정 (8/22)

- 배지 대상은 A군 63장으로 한정. B군은 제외
  근거: 희귀도만 바뀐 카드는 성능이 그대로라 승률 데이터가
  오염되지 않음. "데이터 부정확" 문구가 사실과 다름.
  등장 빈도로 인한 상대 가치 변화는 별개 사안으로 분리
- S 3장은 배지가 아니라 손으로 티어 수정 (아래 다음 단계 1번)
- 가중치 조정은 배지 구현 후 재평가. 지금 결정하지 않음
  근거: DELTA에서 뺀 몫을 Codex로 보내면 같은 승률 데이터라
  무의미하고, 티어표로 보내면 그 티어표가 v0.108 기준이라
  이번 변경들을 모름. 옮길 깨끗한 통이 없음

■ ⚠️ 실물로 확인된 오작동

TODO 389~392행의 손수 정리한 v0.109 너프 목록과 대조:
- CRIMSON_MANTLE 8(10)→7(10) 너프 → 현재 ironclad S
- COLOSSUS 방어5(8)→4(7) 너프 → 현재 ironclad A
"너프된 카드가 아직 좋게 뜬다"가 추측이 아니라 실제 사례로 확인됨
단, 389행 목록은 v0.109 기준이고 betaDiff는 v0.111.0이라
같은 변경인지 누적인지 패치노트 재확인 필요

■ 다음 단계 (순서대로)

1. S 3장 + COLOSSUS 패치노트 대조 → 너프면 db.js 티어 수동 하향
   자료: sts2.gg/patch-notes, sts2companion.com/patch-notes
   TRANSFIGURE, MANGLE, CRIMSON_MANTLE, COLOSSUS

2. event 카드 4장(WHISTLE, MAD_SCIENCE, BRIGHTEST_FLAME, RELAX)이
   일반 카드 보상 화면에 실제로 등장하는지 확인
   → 등장 안 하면 처리 대상에서 제외

3. MAD_SCIENCE는 db.js 미등재라 전문가 앵커 클램프를 건너뜀
   → 아래 6번(천장 A 처방)을 구현하면 자동 해소됨. 6번과 묶어 처리

4. A군 63장 배지 표시 구현
   문구안: "최근 패치에서 바뀐 카드예요. 승률 데이터에 변경 전
   기록이 섞여 있어 점수가 부정확할 수 있습니다"
   표시 위치·아이콘 미정

5. 배지 적용 후 체감 보고 DELTA 가중치 재평가

■ 참고: 자동 방향 판별 불가 (8/22 확인)

betaDiff.changed는 카드키 → 바뀐 필드명 문자열 배열 구조이고,
변경 전/후 값이 없음. cards에는 변경 후 값만 저장.
따라서 버프/너프 방향을 자동으로 알 방법이 없고,
패치노트 수동 대조(A안)만 가능. 전수는 비효율이므로
S/A 등급 카드로 좁혀서 적용

**3. 상점 카드별 스킵** — 친구 피드백 중 미처리
현재 스킵 버튼(index.html의 skip-btn)은 후보 전체를 비운다.
보상은 한 장 고르면 나머지가 사라지므로 맞지만, 상점은 여러 장 살 수 있어
"이 카드만 안 산다"가 안 된다. 슬롯의 ✕로는 가능하나 추천 목록에서 멀어 발견이 어려움.
친구에게 "카드별로 빼고 싶었던 것이 맞는지" 확인 후 진행할 것.

**4. 히스토리 경로 안내 보완**
프로필명 폴더는 사람마다 있고 없다. 실제 확인:
- 친구: ...\profile1\(프로필명)\saves\history
- 정쀼쨔: ...\profile1\saves\history  ← 프로필명 폴더 없음
현재 안내는 항상 있는 것처럼 쓰여 있어, 없는 사람이 헤맬 수 있음.
"있을 수도, 없을 수도 있습니다" 정도로 보완 필요.

**5. max_hp 추가 (작음)**
사망 분석 1층이 "체력 36으로 시작해"까지만 표시된다. "36/70"으로 하려면
tools/build_run_history.js의 slim()에 max_hp를 추가하고 재빌드해야 한다.
재빌드는 .run 원본이 있는 PC에서만 가능.

**6. 전문가 앵커 가드레일 사각지대 — 신규 카드 17장** (우선순위: 중 — 급하지 않으나 구조적)
db.js 미등재 카드는 expertClampVal(±1.0 클램프)을 건너뛰어 DELTA 단독 채점됨.
8/19 전수 대조에서 확인:
- 17장 중 15장이 S(4.57~5.00), Δ +21~+43%p — 앵커 없이 만점 수렴
- 17장은 v0.111.0 베타 신규 19장에서 ABUNDANCE/DOWSING을 제외한
  나머지와 정확히 일치
- 완충: 17장 중 16장이 multiplayer_only=true라 솔로 보상 화면 미노출.
  실질 노출은 SIDESTEP(회피) 1장
- 구조적 문제 — 다음 패치에 신규 카드가 들어오면 동일 현상 재발

**결정된 처방(8/19): 천장 A + 신규 배지 — 미구현**
- db.js 미등재(전문가 앵커 없음) 카드는 최대 A까지만 부여, S 불가
- 근거: S는 "고민 없이 집어"라는 신호라 틀렸을 때 피해가 가장 큼.
  A는 "좋지만 상황 봐서"라 사용자 판단이 개입할 여지가 남음
- 함께 🆕 배지와 "데이터가 적어 부정확할 수 있음" 안내 표시

**7. 패치 변경 카드의 승률 데이터 오염**
- 문제: 승률 데이터에 시점 정보가 없어, 너프/버프 전후 데이터가
  한 덩어리로 평균됨. 예) 너프 전 9,000런 45% + 너프 후 100런 30%
  → 합산 44.8%로 너프가 거의 안 드러남
- 비대칭성: 너프는 사람들이 계속 뽑아서 언젠가 반영되지만,
  버프는 등급이 낮아 안 뽑히고 → 표본이 안 쌓이고 → 등급이 계속 낮은
  자기강화 고리에 갇힘. 우리 앱이 이 고리를 강화하는 구조
- 검토한 방법 3가지:
  A) 패치노트 수동 입력 — 정확하나 패치마다 손이 감
  B) 외부 사이트 자동 수집 — 손은 안 가나 이름 매칭 취약,
     남의 사이트 구조 변경에 취약
  C) 방향 없이 신뢰도만 하향 — betaDiff.changed 카드의 DELTA 비중을
     낮추고 전문가 티어 쪽에 더 의존
- 잠정 결론(8/19): **C부터 시작하고 나중에 A를 얹는다.**
  C만으로도 "너프된 카드가 S로 뜨는" 급한 문제는 막힘
- 선행 조사 필요: betaDiff.changed가 변경 방향(버프/너프)까지
  담고 있는지 확인 → 담고 있다면 A의 난이도가 크게 내려감

**8. 자동 갱신 워크플로 제작 가능**
GitHub Actions에서 spire-codex API 접근 200 확인 완료(8/15).
요청 제한은 엔드포인트마다 다름: /cards 300, /beta/diff 120, /runs/ 계열 60 (분당).
/runs/가 가장 빡빡하므로 통계 수집 속도는 여기에 맞춰야 함.

### 홍보 전 남은 것

- [x] **푸터 버전 표시** (2026-08-18 완료, PR #35)
- [ ] **Firefox 동작 확인** — #22에 "해결됨"이나 그 뒤 변경이 많음. 재확인 필요
- [ ] **구글 폼 문항 정리** — 제목에 `(객관식, 필수)` 등 초안 메모 잔존. **코드 아님**
- [ ] **첫 화면 3초 파악 확인** — 폰 첫 화면만 보고 뭐하는 사이트인지 아는가
- [ ] **디시 「슬더스2 공략 및 정보글 모음」 등록 요청** — 목차 1번이
      이미 "덱 추천 툴" 카테고리. 개발 없이 되는 가장 싼 한 수

### 기록만 — 급하지 않음

- **`osty_atk` vs `osty_attack` 중복 의심**(8/16) — db.js에 둘 다(7곳/5곳).
  오타로 갈린 거면 태그가 반쪽만 잡혀 추천 정확도에 영향.
  → "실제 참조 위치를 찾아서 보고" 요청할 것.
  같은 계열 사례(8/19): **MULTI_CAST 중복 확인됨** — db.js에 MULTICAST(B)와
  MULTI_CAST(A)가 동시 존재, 티어 불일치. 실사용은 MULTI_CAST(A),
  MULTICAST는 도달 불가 고아 키. → db.js 전반의 중복 키 전수 점검 필요.
- **유령 id SCARE**(8/19) — extra_cards.js에 게임 추출본으로 등재돼 있으나
  API 카드 목록 595장에 없음. upstream scores 501종에는 존재.
  upstream이 카드 목록에서 누락한 것으로 보임.
- **패치노트 소스**(8/19, 다음에 할 일 7번 참고 자료)
  - https://sts2.gg/patch-notes — 카드/유물 변경을 diff 형태로 정리
  - https://www.sts2companion.com/patch-notes — 패치별 변경 분류·건수
  - 확인된 사실: ABUNDANCE(풍요)는 v0.110.0에서 버프됨
    (항상 업그레이드된 파워 생성 + 업그레이드 시 에너지 -1).
    DOWSING/DOWSING_ROD는 v0.109.0 신규.
    → 8/19 관측한 풍요 승률 42.9%는 선택편향 + 실제 버프가 겹친 값
- **현재상태.md 3행 날짜** — 날짜만 고치면 또 낡음. 헤더에서 날짜 빼는 쪽 검토.
- **푸터 날짜는 표준시(UTC) 기준** — `generatedAt.slice(0,10)`.
  `build_from_api.js` 237행의 헤더 주석과 같은 기준을 쓰려고 일부러 맞춘 것.
  한국시간 새벽 0~9시에 갱신하면 하루 전 날짜로 찍힌다(정상).
  바꾸려면 index.html이 아니라 build_from_api.js 한 곳을 고칠 것.

### 홍보 계획 (앱 준비 끝난 뒤)

핵심: "홍보글"이 아니라 **"정보글"**. 겸손 + 만든 이유 + 한계 인정.
**Ko-fi 링크는 첫 글에 절대 넣지 않는다.** 가장 큰 지뢰.

순서: 아카라이브 등 작은 곳(반응 테스트) → 수정 → 디시 본진(패치 직후 저녁 8~11시)
→ 공략글 모음에 등록 요청 → 레딧

디시: **슬레이 더 스파이어 마이너 갤러리 `id=slay`** 가 본진.
2편 전용 갤러리(`slaythespire2`)는 한산해서 안 됨.
그 갤러리의 「슬더스2 공략 및 정보글 모음」 정리글 목차 1번이 이미
"덱 추천 툴" 카테고리 → **등록 요청이 가장 효율 높은 한 수.**
외부 링크가 스팸 필터에 걸리면 댓글에 달 것.

레딧: 한국어 전용이라 영어권 반응은 미지근하다.
**"제품 소개"가 아니라 "점수 계산 방식 공유"라는 기술 정보글**로 접근.
3원 블렌드·표본 가중치·전문가 앵커 가드레일은 영어권에서도 흥미로운 주제.
⚠️ 레딧 전 필수: **ptrlrd(spire-codex)에게 미리 한마디 해둘 것.**
"남의 데이터 쓰고 출처 안 밝힘"이 레딧에서 제일 크게 터지는 사고다.

✅ 완료 (2026-08-25) — ptrlrd/spire-codex#891
   https://github.com/ptrlrd/spire-codex/issues/891
   내용: 트래픽 증가 예고 + 이미지 CDN 핫링크 문의 + 출처 표기 문의
   확인한 사실: API는 API_TERMS.md 적용(PolyForm 아님),
   커뮤니티 사용 무료, 출처 표기는 권장이며 필수 아님
   → 레딧 글보다 날짜가 앞서므로 "사전 통보" 증거로 사용 가능
   ⏳ 회신 대기: 이미지 자체 호스팅 전환 여부 (레딧 일정에 영향)

✅ ptrlrd 회신 완료 (2026-08-25, 이슈 891 closed)
   - CDN 사용 명시 승인. 광고 수익으로 충당 → **자체 호스팅 전환 불필요**
   - spire-codex 방문자의 약 5%가 한국 → 한국어 사용자 우선 홍보 근거
   - Showcase 등록 제안받음 (영어화·버그수정 없이 등록 가능)
   - 디스코드: discord.gg/xMsTBeh

✅ Showcase 등록 요청 완료 (2026-08-29)
   디스코드 DM(ptrlrd 본인이 DM 권장) + 이슈 891에 후속 댓글
   회신 대기 중

### 작업 절차 (매번 동일)

1. Claude Code 지시서 끝에 "새 브랜치를 만들어서 작업해줘" 붙이기
2. 큰 변경은 "적용 전에 계획/diff 먼저 보여줘" 요구
3. 승인 → 커밋 → 즉시 push → 해시 확인
4. PR: `https://github.com/thecaous1-hash/arcana-codex/compare/main...브랜치이름`
   (GitHub Desktop의 Preview Pull Request는 base가 꼬일 수 있음)
5. 머지 → PR 화면에서 Delete branch
6. GitHub Desktop: main으로 전환 → Fetch origin → Pull

- **한 작업 한 커밋.** 코드 수정과 데이터 갱신은 반드시 별도 커밋
  (데이터 파일 476KB가 코드 diff를 덮어버림)
- 브랜치 재사용 금지 — base 기준점이 꼬여 PR 생성 실패
- 클라우드 세션은 main 직접 푸시 금지, 브랜치까지만
- .run 원본이 필요한 작업(build_run_history.js)은 PC에서만 가능
- Claude Code Stop 훅의 커밋 작성자 이메일 변경 요구는 **무시**
- 데이터 갱신: `npm run build-data` (NODE_USE_ENV_PROXY=1 필요, 10~20분).
  클라우드 세션은 네트워크 정책 허용 목록에 spire-codex.com 등록 완료

---

> 2026-07-05 갱신. 항목 #1~#25를 실제 코드와 하나씩 대조해 정리했습니다.
> 미완은 **#11 자동업데이트**(웹앱 한계 → Tauri 단계) 하나뿐. #15~#25는 모두 반영·검증 완료.
> 다음 큰 작업은 설계 **§3 데이터 기반 추천 두뇌**.

---

## ✅ 완료

### 데이터 정확도
- ☑ **#1·#10 카드/유물 설명 숫자(◆) 바인딩** — ◆가 카드 269→**14개**(95%↓), 유물 58→**10개**로 감소.
  - 게임 C# 소스에서 수치 재파싱(`parse_card_stats.js` 버그 수정 + `parse_relic_stats.js` 신규).
  - 설명 렌더에 SmartFormat 해석기 도입(조건부 `{IfUpgraded}`/`{InCombat}`, 아이콘 `⚡`/`★`, `{CalculatedX}`→기본값, choose 단·복수).
  - **잔여 14개**: "X장"처럼 고정 숫자가 없는 스케일링·계산형 카드 → ◆가 자연스러운 경우. (원하면 개별 처리 가능)
- ☑ **#3 네크로바인더 시작 덱** — 게임 `StartingDeck` 원문 대조. Soul Spark(유령카드) 제거 → 호위·풀어놓기.
  - **보너스**: 리젠트 시작덱도 오류(별똥별 누락) 발견·수정.
- ☑ **#2·#4 카드 이미지 최신화 + 캐릭터별 기본 카드**
  - GDRE로 게임 v0.108.0 카드 아틀라스 재추출 → 타격/수비를 **캐릭터별 그림**으로(5캐릭 × 2 = 10개).
  - "옛날 이미지"의 실체 없음: 데이터·이미지 모두 현재 게임(v0.108.0)과 일치. 그림 없는 18개는 **게임에 존재하지 않는 STS1 잔재 카드**(추출로도 못 채움).

### 화면 / UX
- ☑ **#5 유물 설명 표시** — 유물을 타일로(아이콘+이름+등급+효과설명). 시작유물 🔒고정, 나머지 ✕제거.
- ☑ **#7 카드 타입 배지** (공격/스킬/파워/상태이상/저주)
- ☑ **#8 카드 강화 전/후 수치 표시**
- ☑ **#9 덱 카드 강화 토글 버튼**
- ☑ **#12 카드 클릭 상세 팝업** — 큰 이미지 + 이름 + 배지 + [기본/강화 후] 토글 + 한글 설명.
- ☑ **#6 보상 입력영역 전체목록 안 펼침** — 이미 그렇게 동작(그리드는 버튼으로만 열림).
- ☑ **한글 초성 검색** (ㅍㅅ→포식). 공식명·추정명 둘 다 대조.
- ☑ **오른쪽 유물 줄 + 추가 칸**

### 도구 / 인프라
- ☑ **#13 git-credential 오류** — 윈도우 자격증명 관리자(VaultSvc) 서비스를 "자동"으로 전환해 해결.
- ☑ 로컬 프리뷰 서버(`serve.js`) + 추출 파이프라인(`tools/`) 정비.

---

## 🟡 부분 완료 (아키텍처 한계)

- 🟡 **#11 자동업데이트 감시**
  - **작동 확인됨**: `check_update.js`가 게임 버전을 감지(현재 v0.108.0 = 추출본 일치 → "최신"). 패치 시 한글 데이터 재추출도 구현됨.
  - **한계**: 브라우저 웹앱은 스스로 게임 파일을 읽거나 백그라운드로 재추출할 수 없음(설계서 §2.3이 명시 — 진짜 자동은 **Tauri(.exe) 단계** 필요).
  - **지금 쓰는 법**: 게임 패치 후 `node tools/check_update.js` 한 번 실행(또는 윈도우 작업 스케줄러 등록)하면 최신 유지.
  - **다음 단계**: Tauri 패키징 시 백그라운드 자동 감시로 승격.

---

## 🎨 추가로 다듬을 것 (선택)

- ☐ **#14 디자인** — 핵심(중립 near-black 팔레트 + 앰버 포인트 + Pretendard 폰트)은 적용 완료.
  더 다듬을 여지: 앰버를 "화면당 2~3곳"까지 더 줄이기, 화면별 여백 미세 조정.
- ☐ **카드 뽑기 → 초기화 동작** — "뽑기" 버튼은 있음. 실제 초기화 흐름 눈으로 최종 확인 권장.
- ☐ 남은 ◆ 14개(스케일링 카드) 개별 문구 처리 — 필요 시.
- ☐ PWA 설치형 → 이후 Tauri(.exe)로 백그라운드 자동감시.

---

## 🆕 새 요청 (2026-07-04 접수 · 2026-07-05 코드 대조 완료)

> 실제 `index.html` 코드와 하나씩 대조한 결과, #15~#22는 그동안의 슬롯/모달 재구성·코스트/강화 데이터 작업에서 **이미 반영**돼 있었고(체크박스만 옛 상태), #23~#25만 남아 있어 **이번에 처리**했습니다. 아래는 검증 근거.

### #15 강화 시 특수 효과 전수 반영 — ☑ 반영됨(대표 카드 눈검 권장)
- 강화로 **코스트 감소**(초록으로 강조)·**키워드 부여**(선천성/소멸/유지/공허 등)를 카드 팝업의 "강화 효과" 줄에 표시. `costCorner()`·`upKeywordsKo()`·`KW_KO`(index.html), 데이터 `data/card_stats.js`(코스트변화 57장·키워드 28장).
- 남은 것: "타격 횟수" 등 일부는 수치(vars)에 녹아 표시됨 — 대표 카드 2~3장만 팝업으로 눈 확인 권장.

### #16 보유 유물 칸 2열로 — ☑ 이미 됨
- `.relic-line{grid-template-columns:1fr 1fr}` + `renderRelics()` 타일 → 2열로 채워짐.

### #17 코스트 표시 강조 — ☑ 이미 됨
- 카드 좌상단 원형 배지 `.cost-cnr` + `costCorner()`. 덱·슬롯·팝업·추천 전부 적용. 강화로 줄면 초록(`.reduced`).

### #18 그리드 검색 목록 비율·열 — ☑ 해결됨(모달 재구성으로)
- 인라인 그리드를 검색 **팝업**(`.pick-grid`, `grid-auto-rows:82px`·이미지 58px)으로 교체 → 찌그러짐 사라짐, 자동 채움 4~5열.

### #19 현재 덱 카드 크기·열 — ☑ 이미 됨
- `.deck-grid{repeat(5,1fr)}` → 한 줄 5개, 이미지 작게.

### #20 X 코스트가 0으로 잘못 표기 — ☑ 이미 됨
- `costDisplay()`가 `cost==='X'` → **'X'** 출력, 음수(-1 등) → 표시 안 함. X카드 10장 데이터 확인.

### #21 강화 추천 카드 초록색 강조 — ☑ 이미 됨
- `.dcell.upnext`(초록 테두리+글로우) + `.dbadge.b-upnext`(초록 ⬆ 배지). `renderDeck()`가 강화 1순위 자동 지정.

### #22 파이어폭스 호환성 — ☑ 해결됨
- 그리드 자체를 슬롯+검색모달로 재구성하며 원인(스크롤 그리드 안 aspect-ratio 붕괴) 제거. 고정 행높이·이미지높이라 크롬/엣지/파폭 동일.

### #23 선택 카드 크기 키우기(정사각형) — ☑ 이번에 처리
- `.slot-img`를 고정높이(52px) → `aspect-ratio:1/1`로 변경 → 슬롯 카드가 **정사각형**(보상 모드 실측 112×112px). 브라우저 계측 확인.

### #24 현재 덱 글자 크기 키우기 — ☑ 이번에 처리
- `.dcell .dn` 글자 `.58rem→.7rem`(실측 9.3→11.2px), 2줄까지 표시(`-webkit-line-clamp:2`).

### #25 하단 범례 색상 통일 — ☑ 이번에 처리
- 범례 각 항목을 실제 배지 색과 1:1로: 강화 추천=초록(75,191,110)·강화 우선 후보=앰버(248,205,122)·제거 추천=빨강(224,80,60)·유물=앰버. 브라우저 계측으로 배지 색 일치 확인.

---

## 🆕 신규 항목 (2026-07-05) — 데이터 층 & 공개 준비

> 방향성 확정 배경은 `어드바이저_설계서.md` §"방향성 확정 (2026-07-05)" 참고.

- [x] **(최우선) spire-codex API 검증 — 완료(2026-07-05). 통계 소스 = spire-codex 확정.**
  - (a) ✅ `GET /api/cards?lang=kor` — **577장 전부 한글**(이름·설명), 설명에 **수치 이미 완성**(예: "피해를 5 줍니다") + `description_raw`에 원본 자리표시자. 구조화 필드 풍부: `powers_applied`(약화 2 등), `is_x_cost`/`star_cost`, `upgrade`/`upgrade_description`, `image_url`. → 내 `db.js`+`card_stats`+`loc`+`card_art`를 **한 소스로 대체 가능**.
  - (b) ✅ 승률 기반 통계. `GET /api/runs/stats?character=…&ascension=…` 의 `top_cards`가 `win_runs`/`total_runs_with` 제공 → **DELTA(승률차) 직접 계산 가능**(검증됨: 강카드 상위·저주/상태이상 하위로 합리적). 단순 픽카운트 아님. 추가로 `GET /api/runs/scores/{cards|relics|potions}`가 **미리 계산된 Codex Score**(Bayesian 보정·ELO·win_rate) 제공. 캐릭터/승천 필터 가능. (표본: 아이언클래드만 14.7만 런)
  - (c) ✅ 최신성. 방금(초 단위 전) 제출된 런이 **build_id v0.108.0** = 내 게임과 동일 버전. `Last-Modified` 오늘. **CORS `Access-Control-Allow-Origin: *`** → 정적 웹앱에서 브라우저 직접 호출 OK(프록시 불필요). rate limit 60req/분.
  - ⚖️ vs ststracker: ststracker.app는 **공개 API 없음**(404). → **spire-codex 단독 채택**(카드+통계 모두).
  - ⚠️ 주의: DELTA는 선택편향 있음(고수가 늦게 얻는 강카드는 과대). → 전문가 티어표로 상위 보정 + 표본수 하한 필요(설계서 방침대로).
- [x] **데이터 층 연결 + DELTA 추천 두뇌 1차 — 완료(2026-07-05, 커밋 ec0ee4d).**
  - `tools/build_from_api.js` → `data/api_data.js` 스냅샷(카드 한글 이름·설명·강화설명 + Codex 티어 + 캐릭터별 승률 DELTA). file:// 유지 위해 스냅샷 방식(갱신=재실행).
  - 카드 사실 API 우선: `koName`/`descHTML`/`descText`/`modalDescHTML` override → **◆ 완전 소멸**, 강화설명 완성본, `[energy:2]`→2⚡. 검색은 i18n 유지.
  - **기본점수 = DELTA**(표본수축 K=1500)로 `logic.js` scoreCard 기본값 교체. 시너지·아키타입·역할 맥락은 그대로. 추천에 "커뮤니티 승률 +X%p · 넣은 런 N판" 표시. (검증: 제물+31.7 > 발화+11.4 > 강타−24.7)
  - 개선 (2026-07-05 모두 완료):
    - ✅ **(i) 전문가 티어표 상위 보정**(커밋 f2ded4c) — 기본점수 = 커뮤니티 DELTA × α + 전문가 티어(db.js) × (1−α), α=n/(n+2000)∈[0.2,0.75]. 추천에 블렌드 비율 표시.
    - ✅ **(ii) 승천별 DELTA**(커밋 003f979) — 전체 / 최고(A10) 밴드 스냅샷 + 상단 '승천' 선택기. A10 표본<200 카드는 전체로 폴백. 추천에 'A10 기준' 태그.
    - ✅ **(iii) 이미지 API 폴백**(커밋 0a883e8) — 로컬 아트 없는 카드/유물은 API CDN(`image_url_card`)로 표시. 로컬은 그대로 우선.
    - ✅ **(iv) 유물 API**(커밋 0a883e8) — 유물 296개 한글 이름·설명 API 우선(`apiRelicOf`/`apiEntity`).
    - ✅ **(v) 티어 배지 S~D 재보정**(커밋 07ac3fe) — 등급 = Codex 커뮤니티 점수(0~100→/20)×0.7 + 전문가 티어(db.js)×0.3. DELTA 단독의 A 쏠림 해소 → S13/A29/B26/C14/D18% 고른 분포. 슬롯·검색(등급순)·덱·추천·팝업 배지 통일. DELTA는 근거로 계속 표시.
  - ✅ **전문가 티어 '출처 표기' UI 완료(2026-07-06)** — 각 추천 카드 근거 하단에 'ⓘ 등급 산정: …' 한 줄 추가. `gradeSourceNote()`(index.html)가 등급 산정 경로를 그대로 반영: Codex 티어 있으면 '커뮤니티 통계 spire-codex 70% + 전문가 티어 Shawnrai 30%', DELTA 보정이면 '커뮤니티 승률 spire-codex + 전문가 티어 Shawnrai 보정', 통계 없으면 '전문가 티어 Shawnrai(db.js)'. 흐린 회색·점선 구분으로 비침투적. 브라우저(Chromium)로 3경로 렌더 검증 완료.
  - ✅ **Codex 미커버 카드 폴백 다듬기 완료(2026-07-06)** — 커뮤니티 통계(Codex 점수·DELTA)가 전혀 없는 카드가 추천에 뜰 때, 근거 하단에 '아직 커뮤니티 통계가 없는 카드예요 — 전문가 티어 Shawnrai 평가만 반영했습니다' 안내문구를 표시(`gradeSourceNote` 3번째 분기 개선 + `isExpertOnly()` 신규 + `.reason-src.nostat` 살짝 강조). 빈칸·데이터 누락으로 오해할 여지 제거.
    - **실측 커버리지(코드 대조):** 카드 풀 581장 중 커뮤니티 통계가 전혀 없는 카드 **42장** = 유령/STS1 잔재 17 + 상태·저주·퀘스트 15 + 전투 중 생성 토큰(단도·영혼·하수인·거대한 바위 등) 10. → **보상으로 실제 뽑히는 일반 카드는 사실상 전부 커버**(무색 특수카드도 캐릭터별 DELTA로 커버됨). 폴백은 수동 입력·토큰·향후 신규 카드 대비 안전망.
  - 다음 후보: 승천 세분(A5 등), 통계 정기 자동 갱신(스케줄러).
- [x] **유령 STS1 카드 검색목록 정리 — 확인 결과 '이미 처리됨'(2026-07-06).**
  - ⚠️ 정정: 직전 메모에 "유령 카드 17장이 검색에 남아 있다"고 적었으나 **사실이 아님**. 그 분석은 `data/extra_cards.js`의 런타임 프루닝을 반영하지 않은 **원본 db.js 기준**이었음.
  - 실제 앱은 로드 시 `data/extra_cards.js`가 `STALE` 목록(ironclad 17장 = Heavy Blade·Clothesline·Wild Strike·Dropkick·Sever Soul·Searing Blow·Limit Break·Spot Weakness·Flex·Reaper·Brutality·Double Tap·Immolate·Intimidate·Disarm·Power Through·Combust + silent Follow Through + regent Child of Stars)을 `DB.cards`에서 `delete`로 제거함.
  - **전수검사(프루닝 적용 런타임 575장):** `API.cards`(STS2 실제 카드)에 없는 유령 카드 **0장**. 브라우저에서 유령 영어명 검색 시 전부 '결과 없음', 이름 비슷한 진짜 카드(Reflex·Reaper Form)는 정상 표시 확인.
  - 코드 변경 없음(불필요). `index.html` pickPool 위에 "유령 제거는 extra_cards.js가 담당" 설명 주석만 추가.
- [x] **개명 완료(2026-07-05)** — 프로젝트명 "Spire Codex" → **"Arcana Codex"**(spire-codex.com/ptrlrd 충돌 회피). 바꾼 곳: 앱 헤더·`<title>`, README 제목, package.json(신규), 설계서/TODO/현재상태 제목, launch.json. **외부 데이터 소스(spire-codex API/크레딧·build_from_api.js)는 그대로 유지.** ⚠️ GitHub 레포명은 사용자가 Settings에서 `arcana-codex`로 변경 필요(gh CLI 미설치).
- [x] **내 GDRE/ILSpy 추출 파이프라인 → '백업' 역할로 정리 완료(2026-07-06).**
  - 각 백업 스크립트(8개: `pck_extract`·`extract_loc`·`parse_card_stats`·`parse_relic_stats`·`crop_card_art`·`crop_basic_cards`·`extract_missing_cards`·`check_update`) 헤더에 `[역할: 백업(2차 소스)]` 표시.
  - 주력(`build_from_api.js`)=`[역할: 주력(1차 소스)]`, 런복기(`build_run_history.js`)=`[역할: 런 복기 전용]`로 구분 표기.
  - `tools/README.md` 신설 — 세 갈래(주력/백업/별개기능) 지도 + 각 스크립트 역할·사용법.
  - 파일은 이동하지 않음(상대경로 유지). 문서·주석만 정리 → 앱 동작 변화 없음.
- [~] ⚠️ **전문가 티어 버전 지연 — 1차 갱신 완료(2026-07-06), 일부 잔여**
  - **질문:** 전문가 티어 출처가 어디이고, 티어·커뮤니티 데이터가 게임 버전과 맞는지?
  - **출처:** 전문가 티어 = **Shawnrai의 STS2-Advisor-Y(`db.js`)** 손수 티어 + 이 포크가 패치노트로 부분 갱신. `db.js` **파일 안엔 출처 주석 없음**(출처는 README Credits·설계서·CHANGELOG에 기록). 커뮤니티 데이터 = spire-codex API.
  - **버전 정합성 (핵심):** 카드 사실·텍스트·이미지·커뮤니티 승률(spire-codex) = **v0.108.0**(현재 게임과 일치 ✅). 그러나 **전문가 티어(`db.js`)는 v0.104까지만 반영**(v0.105~0.108 미반영, grep으로 확인). → **약 4패치 뒤처짐.**
  - **영향:** 등급의 30%(전문가 티어)가 옛 밸런스 기준. 나머지 70%(커뮤니티 승률)는 최신이라 부분 완충되나, 이전에 느낀 "티어가 좀 이상한데?"의 유력 원인일 수 있음.
  - **✅ 1차 갱신 완료(2026-07-06):** 사용자가 v0.105~0.108 패치노트 제공 → 패치 방향 + 커뮤니티 데이터 교차확인된 **12장 티어 변경**(군주의 시선 D→A, 잉크 칼날 D→A, 검성/불굴/로열티 B→A, 사신의 낫 C→B, 초승달창/고정시키기/난동 D→C, 반사 S→A, 산산조각 A→B, 별의 파동 B→C). 12/12 검증·앱 렌더 정상. 상세는 CHANGELOG.
  - **✅ 2차 갱신(2026-07-06): D 방치 카드 싱글/멀티 선별 완료.** db.js D/F인데 커뮤니티 A/S인 39장을 분류 → **멀티 전용 18장은 D 유지**(측면공격·해골군단·희망의 등불 등, 아군 대상이라 솔로 무용 = D가 맞음), **싱글 저평가 21장은 커뮤니티 −1단계로 상향**(S→A 7장, A→B 14장: 사신의 형상·우주 먼지·궤도·환영검 등). 상세는 CHANGELOG.
  - **잔여(다음 후보):** ① 수치/메커니즘만 바뀐 카드 노트 갱신(티어는 유지) ② 덮쳐!(패치 버프 vs 커뮤니티 저평가 — 방향 재검토) ③ db.js에 출처·기준버전 주석 헤더 추가.
- [ ] ⏸️ **v0.108 이후 베타(~v0.109) 반영 보류 (2026-07-06)** — 커뮤니티 데이터(spire-codex)가 아직 v0.108이라, 전문가 티어만 앞서가지 않도록 **커뮤니티 스냅샷이 이 버전 될 때 함께 반영** 예정. 싱글플레이 카드 변경 목록:
  - 버프: Howl From Beyond 16→18, Setup Strike 힘2(3)→3(4), Anticipate 민첩2(3)→2(4), Flick-Flack 6→7, Devastate 30(40)→35(45), Resonance 별비용3→2, Haunt HP손실6(8)→7(9), Reave 9(11)→10(13), Soulstorm 추가피해2(3)→4(6), Momentum Strike 10(13)→11(15)
  - 너프: Colossus 방어5(8)→4(7), Crimson Mantle 8(10)→7(10), Tracking(공격 2배→50%↑), Beacon of Hope 비용1→2, Compact의 Fuel토큰(드로우 제거)
  - 리워크/희귀도: Outbreak(독 매번 AoE 3(4)), Shatter 이제 소멸, Flanking Uncommon→Rare, Ignition Rare→Uncommon, Legion of Bone 소멸 제거
  - 멀티플레이 전용 신규카드 15종(Midnight/Blade Symphony 등)은 싱글 어드바이저 무관 → 제외.
- [ ] ⏸️ **v0.109.0 패치 반영 (2026-07-17 출시) — 데이터 소스 대기 중**
  - **상태:** 대기 — spire-codex가 아직 v0.108.0 기준 (2026-07-18 확인).
  - **선행 조건:** spire-codex 데이터가 v0.109 기준으로 갱신 + 승률 표본이 충분히 쌓일 것.
  - **확인 방법:** https://spire-codex.com/news 에서 최신 패치 표기 확인.
  - **반영할 내용:**
    - 카드 너프/버프 다수 (밸런스 변경).
    - Dominate(아이언클래드): 언커먼 → **레어** 등급 변경. ⚠️ 등급 변경은 등장 빈도가 바뀌어 언커먼 풀 전체 상대 가치에 연쇄 영향 → 단순 수치 수정이 아니라 티어 재검토 필요.
    - Pillar of Creation(리전트): 카드 생성 시마다 3 블록 → 턴당 1회 5 블록.
    - 신규 Neow 렐릭 추가 (Dowsing Rod 등).
    - 출처: https://slaythespire.wiki.gg/wiki/Slay_the_Spire_2:V0.109.0_-_Beta_Patch
  - **⚠️ 주의:** 스코어링이 3원 블렌드(DELTA 0.45×표본신뢰 + 전문가 0.30 + Codex 0.25 — 2026-07-25 개편) 구조라, 커뮤니티 데이터(Codex·DELTA)가 v0.109 기준으로 쌓이기 전에 전문가 티어만 먼저 고치면 축이 어긋남. (line 141 베타 보류 항목과 동일 원리)

### 추천 엔진 수술 로드맵 (2026-07-25 진단 기반 — 회귀 검증: `node tools/verify_blend.js`)
- [x] **1차 — 기본점수 3원 블렌드 (2026-07-25 완료)** — 구 산식(Codex 0.7+전문가 0.3, DELTA 미조회)의 낡은 Codex 독주 제거. 신규: 가중평균(DELTA 0.45×표본신뢰 n/(n+1000) · 전문가 0.30 · Codex 0.25, 없는 신호 재정규화). 검증: 역전 2쌍→1쌍, Bloodletting 기본 B→A·최종 S, 극단 이동 0, 보정 층 불변. 커밋 df08996(산식)·e334fe3(하네스).
  - 📝 **재평가 메모 — C구간 축소(DELTA +편향):** 새 블렌드로 C등급 100→45장(중간 카드 완만 상향). 일단 현행 유지, **실사용 체감 + 2차 수술(재수집) 후 재평가** — 과하면 `BLEND_N_CONF` 또는 DELTA 정규화(`2.5+Δ/12`) 조정 논의.
- [ ] **2차 — 데이터 재수집·기간 필터** — 스냅샷이 2026-07-05 1회 수집 후 동결 + API 집계가 전기간 누적(3월 v0.100~, Bash 표본 88%가 증거)이라 현 메타(무한 콤보) 미반영. 재수집 주기화(`check_update.js` 연계), 가능하면 최근 기간·현재 버전 런 기반 델타 재산출(`runs/list` 자체 집계 검토). **잔여 역전 1쌍(Demon Form>Pommel Strike)의 해소 조건.**
- [ ] **3차 — 1막 컨텍스트 보정** — 진단 확인 사항: 방어 프리미엄이 sin 곡선이라 1막 초반 0, aoe 태그 로직 전무, 1막 유일 보정이 엔진 +0.3(스케일링 우대). 방어 곡선 초반 프리미엄·aoe 1막 가점·엔진 가점 방향 재검토 + 맥락 보정 총량(현 층+막 ≤ ±1.0) 확대 검토.
  - 📌 **근거 자료 확보 (2026-08-30)** — DC 슬레이더스파이어 갤러리 1막 공략.
    핵심 주장 3가지:
    ① 티어표는 "게임을 이겨 주는 카드" 기준이라 1막에 그대로 쓰면 죽는다.
       1막에 필요한 건 "살아남게 해주는 카드".
    ② 초반에는 시너지를 붙일 수 없으므로, 덱 자리를 적게 먹으면서
       조건 없이 즉발로 강한 카드가 최강. (예: 잿불 2코 18딜 무조건 >
       강타→녹주→협박 콤보 1코 20딜, "원하는대로 드로우 될 리가 없기에")
    ③ 1막은 한 덩어리가 아니라 네 구간(지도·니오우 / 쉬운 전투 /
       첫 엘리트·모닥불 / 덱빌딩 시작)이고 구간마다 판단 기준이 다르다.
  - 📊 **실측 대조 (2026-08-30)** — 1막 3층·아이언클래드 기본덱(타격5 수비4 강타1)·
    유물 없음 기준으로 현재 main 점수를 전수 계산한 결과, 공략과 정반대였음:
    | 카드 | 공략 평가 | 현재 앱 |
    |---|---|---|
    | 사혈 | 1막엔 타격/수비보다 구림 | 21위 4.42 S |
    | 분노 | 추천 공격 카드 | 70위 3.03 B |
    | 몽둥이질 | 폼멜 5장보다 한 장이 낫다 | 71위 2.95 B |
    | 흘려보내기 | 사혈 대신 먹어라 | 44위 3.87 A |
    상위 20위가 전부 엔진 카드(잔혹함·부동·어둠의 포옹·바리케이드·무통 등).
  - 🔧 **구조적 원인 2곳 (실측으로 특정)**
    ① logic.js 1막 분기 — `data.role === 'engine'` 에 +0.3
       ("1막에 빌드를 세팅하세요"). 공략의 ②와 정면 충돌.
    ② index.html `BLEND_CLAMP = 1.0` — 전문가 앵커 가드레일.
       전문가 티어가 win-oriented라, 사혈(전문가 S) 같은 카드는
       승률이 낮게 나와도 1막에서 S 아래로 내려갈 수 없음.
       → 1막 정확도는 현 설계에서 구조적으로 개선 불가.
  - ⚠️ **검증 필요** — 갤러리 공략 1편이므로 그 자체가 정답은 아님.
    원래의 "숙련자 픽 같지 않다" 불만과 방향이 일치하고 실측으로도
    어긋남이 확인되어 가설로는 유력하나, 착수 전에 1막 초반 구간의
    실제 승률 데이터로 교차 검증할 것.
  - ➡️ **유력 방향** — 앱이 이미 floor를 받고 있으므로 1막을 층수로
    쪼개는 것은 기술적으로 어렵지 않음. 엔진 가점을 1막 전체가 아니라
    후반부 층에만 주는 식. (니오우 유물 루트 의존 선택은 별개 기능 영역)
- [x] **스킵 추천 강화 (2026-07-26 완료)** — 구 이중 문턱(allBad 전원<1.6 / 경고 1위<2.5)은 **발동률 0.3%로 사문** → 단일 문턱 `SKIP_WARN_TH=3.0` 채택(**발동률 4.3%**, 층5 시작 덱×20,000회 시뮬레이션·2막 보조검증 4.1%). **시안2 채택:** 경고 시 1순위 금색 "그래도 뽑는다면 이 카드" + 초록 '①' 해제 + **배너 "⚠ 스킵 권장" 목록 상단 배치**(메시지 모순 해소). 평상시 화면 불변. 커밋 23a9d65·a52255d.
  - [ ] 📝 **희귀도 가정 재검증(잔여):** 채택 문턱 3.0은 보상 슬롯 희귀도 커먼60/언커먼37/레어3(STS1 통설) 가정의 시뮬레이션 근거 — **STS2 실제 희귀도 확률 확인 시 스킵 문턱 재검증.**

### 향후 기능 (배포 후)
- [ ] **피드백 버튼 (설계: `docs/기능_피드백시스템.md`)** — 앱에 열고 닫는 피드백 버튼 → GitHub Issues/구글 폼 링크. 검토는 "사용자 호출 시 Claude Code가 몰아서" 하는 반자동. 배포 마무리 후 구현.

### 인챈트 반영 (3단계 계획 — 설계: `docs/기능_인챈트.md`, 단계별 별도 세션)
- [x] **1단계 — 기록·표시 (2026-07-21 완료)** — 데이터: `api_data.js`에 `enchants` 22종(한글 이름·효과·타입제한, 원본 ptrlrd/spire-codex `kor/enchantments.json`; `build_from_api.js`에 스냅샷 추가, 이미지는 robots 정책상 미사용). UI: 덱 카드 **장별** ✦ 버튼 → 선택 모달(타입 제한·복수타입 지원·1장 1개, 해제 가능), ✦ 배지·툴팁 표시, 기존 `state`/`save()`로 새로고침 유지. **점수 미반영 검증됨**(부여 전후 finalScore 완전 동일). 스냅샷 없으면 기능 조용히 비활성(폴백). 커밋: f81d69e(데이터)·6fa9b59(UI).
- [x] **2단계 — R1+R2+R3 점수·추천 반영 (2026-07-22 완료)** — R1: 인챈트 장 제거 추천 억제(58c73d4) · R2: 증폭 7종 강화 우선 +1.0(36d561e) · R3: 오염→회복·자해 후보 +0.3/장, 상한 +0.6, 총합 캡 ±0.6(88a42a6). 그룹 B/C는 보류(R1만 적용). main(이전 코드) 대조 검증: 무인챈트 덱 완전 불변. 상세: `docs/기능_인챈트.md` §5.
- [ ] ⏸️ **3단계 — 지식 확장·가중치 조율** — 22종 전체 시너지/디스시너지 지식 + 기존 3경로 블렌드 안에서 가중치 조율 + 보상 후보 카드에 붙은 인챈트 입력 검토 + **(2단계 이월) heal 태그 캐릭터 편중 해소**(타 캐릭터 회복 카드 태깅).

### 공개 준비 체크리스트 (배포 시점)
- [x] **비공식 고지**(2026-07-05, 커밋 d88ef63) — 앱 전화면 고정 푸터 + README Notice: "Mega Crit과 무관·게임 에셋은 Mega Crit 저작물". '공식' 인상 표현 금지 준수.
- [x] **라이선스 분리**(d88ef63) — LICENSE에 자산 예외: 소스는 MIT, 게임 유래 에셋(이미지·게임 텍스트)은 Mega Crit 소유·MIT 미포함·팬 콘텐츠 정책하 비영리.
- [x] **제3자 데이터 출처 표기**(d88ef63) — README Credits + 푸터: 데이터 spire-codex.com(ptrlrd) · 기반 코드 yard9/STS2-Advisor-Y(Shawnrai, MIT). (외부 전문가 티어 도입 시 추가 크레딧 예정)
- [x] **Mega Crit 허가 확인**(2026-07-20) — Anthony Giovannetti(devs@megacrit.com) 이메일로 외부 컴패니언 툴 허용·기부 기반 선호 확인 → **Ko-fi 기부 전용·광고 없음 확정**. NOTICE 기록(84917a0) + 푸터 ©·Steam 링크(657573c). 공개 전 정책 블로커 해소.
- [ ] 호스팅: GitHub Pages(무료·정적). ⚠️ 먼저 레포명 `arcana-codex`로 변경(Settings) → remote URL 갱신.
- [x] **후원 링크**(2026-07-28) — **Ko-fi 기부 전용**(광고 없음 — Mega Crit 확인 조건). 계정 개설 완료 → `index.html` 푸터 주석 해제, https://ko-fi.com/thecaous1 활성화.

---

## 💡 나중에 생각해볼 것

- (여기에 떠오르는 아이디어 계속 추가)
